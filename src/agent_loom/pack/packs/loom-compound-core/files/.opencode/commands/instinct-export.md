---
description: Export instincts to a portable bundle.
agent: build
subtask: false
---

Usage:

```bash
loom compound instinct-export --out "$ARGUMENTS" --json
```

If no output path argument is provided, use `.loom/compound/instincts-export.json`.
