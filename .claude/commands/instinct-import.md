---
description: Import instincts from a file or URL.
---

Usage:

```bash
loom compound instinct-import "$ARGUMENTS" --json
```

If confidence conflicts exist, rerun with:

```bash
loom compound instinct-import "$ARGUMENTS" --force --json
```
