---
id: packet:ralph-ticket-0a1106b6-20260502T090349Z
kind: packet
packet_kind: ralph
status: consumed
target: ticket:0a1106b6
mode: execution
change_class: protocol-authority
style: snapshot-first
verification_posture: observation-first
iteration: 1
created_at: 2026-05-02T09:03:49Z
updated_at: 2026-05-02T09:28:53Z
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records: []
  paths:
    - README.md
    - PROTOCOL.md
    - ARCHITECTURE.md
    - skills/loom-bootstrap/**
parent_merge_scope:
  records:
    - ticket:0a1106b6
  paths:
    - .loom/tickets/20260502-0a1106b6-align-drive-public-framing.md
    - .loom/packets/ralph/20260502T090349Z-ticket-0a1106b6-iter-01.md
source_fingerprint:
  git_commit: 633d327aee232442b996b10079589e74e88e9425
  integration_remote: origin
  integration_ref: origin/main
  integration_commit: 633d327aee232442b996b10079589e74e88e9425
  git_status_summary: clean
  compiled_from:
    - initiative:skills-corpus-protocol-sharpening
    - research:skills-corpus-council-review
    - evidence:skills-corpus-council-review
    - plan:skills-corpus-protocol-sharpening
    - ticket:0a1106b6
execution_context:
  branch: main
  push_remote: origin
  worktree: /Users/alexanderbutler/code_projects/personal/agent-loom
  isolation: none
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: forbidden
context_budget:
  posture: normal
  max_source_files: 10
  max_excerpt_lines_per_file: 160
  avoid_full_file_reads: false
sources:
  constitution:
    - constitution:main
  initiative:
    - initiative:skills-corpus-protocol-sharpening
  research:
    - research:skills-corpus-council-review
  spec: []
  plan:
    - plan:skills-corpus-protocol-sharpening
  ticket:
    - ticket:0a1106b6
links: {}
---

# Mission

Align public and bootstrap-facing workflow maps with the shipped Loom skill set,
especially `loom-drive`, and correct README outer-loop framing that currently can
read as a single linear conveyor instead of a backbone plus conditional routes.

# Bound Context

Governing chain:

- `initiative:skills-corpus-protocol-sharpening#OBJ-001`: README plus bootstrap
  should recover the same layer model, canonical/support boundary, and workflow
  map that the skill corpus teaches.
- `research:skills-corpus-council-review#CLAIM-002`: `loom-drive` visibility is
  the most urgent cross-surface drift.
- `research:skills-corpus-council-review#CLAIM-003`: README's linear outer-loop
  diagram can mislead operators.
- `plan:skills-corpus-protocol-sharpening` Wave 1: low-risk public and routing
  alignment before deeper grammar work.
- `ticket:0a1106b6`: live execution owner for this bounded slice.

This is not a shared grammar, packet-contract, ticket-risk, or acceptance-rule
iteration. Do not widen into those later tickets.

# Source Snapshot

Current observations before launch:

- `README.md` includes the outer-loop diagram `research -> spec -> plan -> ticket
  -> evidence -> critique -> wiki -> memory` around the `How Loom works` section,
  while bootstrap doctrine teaches the backbone `constitution -> initiative ->
  plan -> ticket` with research/spec as conditional strengthening and
  evidence/critique/wiki/memory as routed follow-through/support.
- `README.md` skill map enumerates workflow skills but omits `loom-drive`.
- `README.md` says project layers are "canonical layers" and includes `packet`
  and `memory` in the table, so any touched wording must distinguish canonical
  owner layers from durable/support surfaces.
- `PROTOCOL.md` already distinguishes owner layers, support surfaces, execution
  routes, and transport. It does not enumerate the shipped skill list.
- `ARCHITECTURE.md` already distinguishes canonical owner layers from durable
  support surfaces. It does not enumerate the shipped workflow skill list.
- `skills/loom-bootstrap/**` contains routing doctrine but does not need a broad
  rewrite unless a direct route-summary alignment is necessary.

# Change Class

Declared as `protocol-authority` with `risk_class: medium` from the ticket. This
changes public operator framing, so it needs structural evidence and oracle
critique before ticket acceptance. It should not change reusable record grammar.

# Verification Targets

- `ticket:0a1106b6` ACC-001
- `ticket:0a1106b6` ACC-002
- `ticket:0a1106b6` ACC-003
- `ticket:0a1106b6` ACC-004
- `ticket:0a1106b6` ACC-005
- `initiative:skills-corpus-protocol-sharpening#OBJ-001`
- `research:skills-corpus-council-review#CLAIM-002`
- `research:skills-corpus-council-review#CLAIM-003`

# Task For This Iteration

Implement the smallest correct public-framing alignment:

1. Add `loom-drive` to README surfaces that enumerate shipped workflow skills.
2. Describe `loom-drive` as an objective/workflow coordinator that routes through
   owner layers and does not own project truth.
3. Rework the README outer-loop wording so it distinguishes:
   - the backbone `constitution -> initiative -> plan -> ticket`,
   - conditional research/spec strengthening,
   - evidence/critique/wiki follow-through,
   - packet and memory as support surfaces, not canonical owners.
4. Touch `PROTOCOL.md`, `ARCHITECTURE.md`, or `skills/loom-bootstrap/**` only if
   direct drift is discovered that blocks the acceptance criteria.
5. Do not edit shared claim coverage, packet grammar, ticket grammar, risk/change
   class rules, examples, or broad style issues.

# Verification Posture

`observation-first` structural validation.

Before/after evidence should include targeted searches showing:

- before: README skill map omitted `loom-drive` and README outer-loop framing used
  a linear research-to-memory diagram;
- after: `loom-drive` appears in README shipped skill surfaces;
- after: README outer-loop wording no longer presents the outer loop as a single
  linear research-to-memory conveyor;
- after: touched docs distinguish owner layers from support surfaces.

Run `git diff --check` after editing.

# Stop Conditions

Stop and return `blocked` or `escalate` if:

- the work requires editing outside the declared child write scope;
- `loom-drive` visibility requires changing the `loom-drive` skill itself;
- acceptance requires changing shared record grammar, packet semantics, ticket
  acceptance rules, or examples;
- bootstrap doctrine would need a material rewrite instead of a narrow route-map
  alignment;
- source files changed materially after packet compilation;
- destructive commands, Git config edits, commits, pushes, or remote mutations
  seem necessary.

# Output Contract

Return:

- outcome: `continue`, `stop`, `blocked`, or `escalate`;
- files changed;
- records changed;
- concise implementation summary;
- exact validation commands/searches and outcomes;
- before/after observations;
- self-review findings or concerns;
- blockers or residual risks;
- ticket recommendation.

# Working Notes

Parent will reconcile the result into the ticket, packet, evidence/critique as
needed, then run oracle critique before accepting or continuing.

# Child Output

Outcome: `stop`.

Files changed:

- `README.md`
- `skills/loom-bootstrap/references/02-truth-and-authority.md`

Records changed by child: none.

Implementation summary:

- Added `loom-drive` to README shipped workflow skill surfaces.
- Described `loom-drive` as objective/workflow coordination through owner layers
  without owning project truth.
- Replaced README's linear outer-loop conveyor with backbone, conditional route,
  follow-through, and support-surface framing.
- Added a narrow bootstrap routing reference to `loom-drive` as a workflow skill.

Validation returned by child:

- `git diff --check` passed with no output.
- Targeted searches found `loom-drive` in README and bootstrap routing.
- Targeted search found no remaining README occurrence of the old linear
  `research -> spec -> plan -> ticket -> evidence -> critique -> wiki -> memory`
  diagram.

Critique repair:

- Oracle pass `ses_2180d4cc5ffeTGDIyzIom6XHUA` found one non-blocking wording nit
  in the README packet row.
- Follow-up fixer pass resolved the nit by describing packets as durable support,
  not project truth.
- Oracle pass `ses_2180a831affeWru69H99t06o3t` returned `pass` with no findings.

# Parent Merge Notes

Accepted. Parent recorded validation in `evidence:public-drive-framing-validation`
and critique in `critique:public-drive-framing-review`, then closed
`ticket:0a1106b6` with no blocking findings and no residual follow-up needed for
this ticket scope.
