---
id: packet:ralph-ticket-1a12d9ff-20260502T101425Z
kind: packet
packet_kind: ralph
status: consumed
target: ticket:1a12d9ff
mode: execution
change_class: protocol-authority
style: snapshot-first
verification_posture: observation-first
iteration: 1
created_at: 2026-05-02T10:14:25Z
updated_at: 2026-05-02T10:27:05Z
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records: []
  paths:
    - skills/loom-workspace/**
    - skills/loom-bootstrap/**
    - skills/loom-drive/**
    - skills/loom-tickets/**
    - skills/loom-memory/**
    - README.md
parent_merge_scope:
  records:
    - ticket:1a12d9ff
  paths:
    - .loom/tickets/20260502-1a12d9ff-add-workspace-resume-compaction-guidance.md
    - .loom/packets/ralph/20260502T101425Z-ticket-1a12d9ff-iter-01.md
source_fingerprint:
  git_commit: 6524e42835a3db05065f62e5919298abfad159af
  integration_remote: origin
  integration_ref: origin/main
  integration_commit: 6524e42835a3db05065f62e5919298abfad159af
  git_status_summary: clean
  compiled_from:
    - initiative:skills-corpus-protocol-sharpening
    - research:skills-corpus-council-review
    - evidence:skills-corpus-council-review
    - plan:skills-corpus-protocol-sharpening
    - ticket:1a12d9ff
execution_context:
  branch: main
  push_remote: origin
  worktree: /Users/alexanderbutler/code_projects/personal/agent-loom
  isolation: none
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: forbidden
context_budget:
  posture: normal
  max_source_files: 14
  max_excerpt_lines_per_file: 180
  avoid_full_file_reads: false
sources:
  constitution:
    - constitution:main
  initiative:
    - initiative:skills-corpus-protocol-sharpening
  research:
    - research:skills-corpus-council-review
  spec: []
  plan:
    - plan:skills-corpus-protocol-sharpening
  ticket:
    - ticket:1a12d9ff
links: {}
---

# Mission

Make cold-start, post-compaction, and pre-compaction recovery a first-class
workspace-entry route, without creating a new resume ledger or making memory
canonical for correctness.

# Bound Context

Governing chain:

- `constitution:main` says another agent should be able to enter cold and continue
  from visible files, not chat history, private memory, or hidden runtime.
- `initiative:skills-corpus-protocol-sharpening#OBJ-003` requires workspace entry
  and related workflow skills to teach cold-start recovery, compaction recovery,
  scratchpad avoidance, and file-first concurrency without adding ceremony.
- `research:skills-corpus-council-review#CLAIM-004` says cold-start and
  post-compaction resume deserve first-class workspace guidance.
- `research:skills-corpus-council-review#CLAIM-007` says pre-compaction
  checkpoints and related operator ergonomics should improve without creating
  shadow truth.
- `ticket:1a12d9ff` owns this bounded resume and compaction guidance slice.

Tickets remain the sole live execution ledger. Workspace entry may discover and
route live work, but it must not own live execution state. Memory is support-only
recall and may point to owner records, but it must not become required for
correctness.

# Source Snapshot

Current observations before launch:

- `skills/loom-workspace/SKILL.md` currently teaches first read order and routing,
  but it does not directly name cold-start, post-compaction resume, or
  pre-compaction behavior.
- `skills/loom-workspace/references/status-snapshot.md` already groups tickets by
  status and warns when wiki or memory carries owner truth, but it is a status
  synthesis reference rather than a concrete resume route.
- `skills/loom-drive/references/checkpoint-resume-protocol.md` has drive-specific
  checkpoint fields and deterministic resume discovery. Reuse or point to its
  useful pattern without making `loom-drive` the only resume route.
- `skills/loom-memory/SKILL.md` and `skills/loom-memory/references/memory-model.md`
  say memory is optional support recall; deleting memory must not make the
  project story false or incomplete.
- `skills/loom-tickets/references/state-machine.md` says `active`, `blocked`,
  `review_required`, and `complete_pending_acceptance` own live execution state
  until closure.

# Change Class

Declared as `protocol-authority` and `risk_class: high` because this changes how
operators recover work after cold start or compaction. Mandatory oracle critique
is required before acceptance.

# Verification Targets

- `ticket:1a12d9ff` ACC-001
- `ticket:1a12d9ff` ACC-002
- `ticket:1a12d9ff` ACC-003
- `ticket:1a12d9ff` ACC-004
- `ticket:1a12d9ff` ACC-005
- `ticket:1a12d9ff` ACC-006
- `initiative:skills-corpus-protocol-sharpening#OBJ-003`
- `research:skills-corpus-council-review#CLAIM-004`
- `research:skills-corpus-council-review#CLAIM-007`
- `research:skills-corpus-council-review#CLAIM-009`

# Task For This Iteration

Implement the smallest coherent workspace recovery guidance pass:

1. Update `loom-workspace` so cold-start and post-compaction resume are explicit
   workspace-entry behaviors.
2. Teach the route to load bootstrap doctrine, confirm the workspace, read
   `constitution:main`, discover active tickets, follow upstream links, inspect
   evidence and critique as needed, and continue from owner records.
3. Add a practical active-ticket discovery query in the most appropriate
   workspace reference.
4. Add pre-compaction guidance that updates the correct existing owners before a
   session ends or compacts: tickets for live state and next route, evidence for
   observations, critique for findings, wiki for accepted explanation, and memory
   only for support-only recall.
5. Clarify overlap with `loom-drive`: drive can coordinate high-level objective
   resume and checkpoints, but normal workspace entry can resume a bounded ticket
   or status queue without invoking the full drive loop.
6. Add only narrow pointers outside `loom-workspace` if needed. Do not duplicate
   the full route across bootstrap, drive, tickets, memory, or README.

# Verification Posture

`observation-first` structural validation.

Before editing, observe the current guidance shape with targeted searches for:

- `cold-start`, `post-compaction`, `pre-compaction`, `resume`, `active tickets`,
  `chat memory`, `transcript`, and `constitution:main` in the allowed skill
  surfaces.

After editing, run targeted checks for:

- workspace route names cold-start and post-compaction recovery;
- route reads bootstrap doctrine and `constitution:main` before downstream
  records;
- active-ticket discovery and upstream owner-chain follow-through are explicit;
- transcript/chat memory is rejected as canonical truth;
- pre-compaction updates route to existing owner layers rather than a new ledger;
- `loom-drive` remains a coordinator, not the only resume route;
- `git diff --check`.

# Stop Conditions

Stop and return `blocked` or `escalate` if:

- the work appears to require a new canonical resume/checkpoint record kind;
- the work would make memory required for correctness;
- the work would make `loom-drive` or a generated context file own all resume
  truth;
- the route needs a runtime, helper script, command wrapper, or hidden index;
- editing public docs becomes more than a short pointer;
- editing outside child write scope seems necessary;
- destructive commands, Git config edits, commits, pushes, or remote mutations
  seem necessary.

# Output Contract

Return:

- outcome: `continue`, `stop`, `blocked`, or `escalate`;
- files changed;
- records changed;
- exact cold-start / post-compaction recovery route added;
- exact pre-compaction owner-update guidance added;
- summary of any `loom-drive`, ticket, memory, bootstrap, or README overlap
  changed or intentionally deferred;
- exact validation commands/searches and outcomes;
- self-review findings or concerns;
- blockers or residual risks;
- ticket recommendation.

# Working Notes

Parent will run oracle critique with protocol-change, operator-clarity, and
routing-safety profiles before accepting or continuing.

# Child Output

Outcome: `stop`.

Files changed by child:

- `skills/loom-workspace/SKILL.md`
- `skills/loom-workspace/references/status-snapshot.md`

Records changed by child: none.

Recovery route added:

- Load `loom-bootstrap` doctrine or confirm preloaded bootstrap references.
- Confirm workspace root and `.loom/` tree.
- Read `constitution:main`.
- Discover active and live ticket queues from ticket records.
- Read relevant upstream owner chains and cited support records.
- Inspect evidence and critique as needed for acceptance, review, blocker, or
  next-route disposition.
- Continue from owner records rather than chat history, transcript memory,
  generated context, external trackers, or memory files.

Pre-compaction owner-update guidance added:

- tickets own live status, blockers, progress, dispositions, and next route;
- evidence owns observed outputs and validation artifacts;
- critique owns findings, verdicts, risks, and required follow-up;
- wiki owns accepted explanation and workflow knowledge;
- memory owns optional support-only recall or pointers only.

Validation reported by child:

- targeted pre/post searches for cold-start, post-compaction, pre-compaction,
  resume, active tickets, chat memory, transcript, and `constitution:main`;
- `git diff --check` passed with no output;
- `git status --short` showed only child-edited workspace files plus expected
  parent-owned `.loom` changes.

Ticket recommendation: move to `review_required` and run mandatory oracle
critique before acceptance or closure.

# Parent Merge Notes

Accepted for review. Parent recorded validation in
`evidence:workspace-resume-compaction-validation`. Initial oracle critique found
parent-side record reconciliation and stale-evidence issues, not product-text
blockers. Parent updated the packet, ticket, and evidence before requesting final
oracle re-check. Final oracle critique passed with no new findings, and parent
closed `ticket:1a12d9ff` with the residual risks recorded in the ticket and
critique.
