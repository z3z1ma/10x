---
id: "packet:ralph-ticket-7h8u6oxp-20260507T222124Z"
kind: packet
packet_kind: ralph
status: consumed
target: "ticket:7h8u6oxp"
mode: execution
change_class: config
risk_class: high
style: reference-first
verification_posture: observation-first
iteration: "01"
created_at: "2026-05-07T22:21:24Z"
updated_at: "2026-05-07T22:26:09Z"
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - "None - child returns implementation output only; parent reconciles Loom records"
  paths:
    - .claude-plugin/**
    - .codex-plugin/**
    - .cursor-plugin/**
    - .agents/plugins/marketplace.json
    - claude-hooks/**
    - gemini-extension.json
    - gemini-bootstrap.md
    - loom-core/.claude-plugin/**
    - loom-core/.codex-plugin/**
    - loom-core/.cursor-plugin/**
    - loom-core/claude-hooks/**
    - loom-core/gemini-extension.json
    - loom-core/gemini-bootstrap.md
    - loom-playbooks/.claude-plugin/**
    - loom-playbooks/.codex-plugin/**
    - loom-playbooks/.cursor-plugin/**
    - loom-playbooks/claude-hooks/**
    - loom-playbooks/gemini-extension.json
    - loom-playbooks/gemini-bootstrap.md
parent_merge_scope:
  records:
    - ticket:7h8u6oxp
    - evidence:harness-package-surface-skeleton-check
    - critique:harness-package-surface-skeleton-review
  paths:
    - .loom/tickets/20260507-7h8u6oxp-rebuild-harness-package-surfaces.md
source_fingerprint:
  git_commit: d021bc1c4968f398f6ca7a666e006b4cced6d666
  integration_remote: origin
  integration_ref: origin/main
  integration_commit: d021bc1c4968f398f6ca7a666e006b4cced6d666
  git_status_summary: dirty_mixed
  git_status_detail: "closed prior split tickets and untracked package-root skill moves are present; unrelated untracked examples remain"
  compiled_from:
    - ticket:7h8u6oxp
    - spec:core-and-playbooks-package-contract
    - plan:split-core-and-playbooks-packages
    - decision:0008
execution_context:
  branch: main
  push_remote: none
  worktree: /Users/alexanderbutler/code_projects/personal/agent-loom
  isolation: none
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: forbidden
context_budget:
  posture: normal
  max_source_files: 20
  max_excerpt_lines_per_file: 120
  avoid_full_file_reads: true
sources:
  constitution:
    - decision:0008
  spec:
    - spec:core-and-playbooks-package-contract
  plan:
    - plan:split-core-and-playbooks-packages
  ticket:
    - ticket:7h8u6oxp
    - ticket:u9vtemj3
links:
  decision:
    - decision:0008
  ticket:
    - ticket:u9vtemj3
---

# Mission

Create the common non-OpenCode native harness package skeleton for the split Loom
package roots.

The repository already has `loom-core/skills` and `loom-playbooks/skills`. This
iteration must make Claude, Codex, Cursor, and Gemini metadata/catalog surfaces
structurally point at those package roots instead of the retired repository-root
`skills/` product surface.

# Bound Context

Governing source truth:

- `decision:0008` authorizes `loom-core/` and `loom-playbooks/` as the product
  package roots and retires root `skills/` as a product surface.
- `spec:core-and-playbooks-package-contract#REQ-002` requires each package root to
  contain native plugin, extension, hook, package, or metadata files needed to
  expose that package.
- `spec:core-and-playbooks-package-contract#REQ-009` allows root catalogs to list
  both package roots but forbids them from owning Loom semantics.
- `spec:core-and-playbooks-package-contract#REQ-011` and `#REQ-012` prohibit Codex
  and Gemini preload/install claims without runtime evidence.
- `ticket:7h8u6oxp` excludes OpenCode package work and public docs.

Current root surfaces to inspect:

- `.claude-plugin/plugin.json`
- `.claude-plugin/marketplace.json`
- `claude-hooks/hooks.json`
- `.codex-plugin/plugin.json`
- `.agents/plugins/marketplace.json`
- `.cursor-plugin/plugin.json`
- `gemini-extension.json`
- `gemini-bootstrap.md`

# Parent Launch Checklist

- Target ticket is active and Ralph-ready.
- Child write scope is limited to harness metadata/catalog/preload surfaces named
  in packet frontmatter.
- Parent retains Loom record reconciliation.
- Network and destructive commands are forbidden.
- Verification posture is observation-first with before/after structural checks.
- OpenCode package files are out of scope.

# Source Snapshot

Before editing, inspect the current root harness files listed above and the new
package-root skill directories:

- `loom-core/skills/using-loom/references/`
- `loom-core/skills/*/SKILL.md`
- `loom-playbooks/skills/*/SKILL.md`

# Assumptions / Decision Triggers

| Assumption or question | Reversible? | Blocks child work? | Disposition |
| --- | --- | --- | --- |
| Existing root manifest shapes are acceptable structural starting points for package-root manifests. | yes | no | accepted |
| `loom-core` owns using-Loom preload/context files; `loom-playbooks` should not duplicate core preload doctrine. | yes | no | accepted |
| Root catalogs can list two local package roots in the same catalog format already present. | yes | no unless contradicted by current file shape | accepted |
| A catalog format cannot honestly express two package roots. | yes | yes | stop and report instead of inventing a format |
| OpenCode files need changes. | yes | yes | stop and leave for the OpenCode ticket |

# Change Class

Declared as `config`: this changes plugin, extension, hook/context, and catalog
metadata. Risk is high because package metadata controls install and preload
behavior and can misrepresent root `skills/` retirement.

# Quality Delta

Baseline/current state:

- Root plugin manifests point at `./skills/` even though root `skills/` has been
  retired.
- Root catalogs list one repository-root package.
- Gemini bootstrap references `./skills/using-loom/...` from the repository root.

Expected improvement:

- `loom-core` and `loom-playbooks` each have local package-root metadata for
  Claude, Codex, Cursor, and Gemini where structurally supported.
- Root catalogs list both package roots as discovery surfaces.
- Core preload/context files point at `loom-core/skills/using-loom` from inside
  the core package root.
- Playbook package metadata names the dependency on `loom-core` without copying
  core doctrine or preload references.
- Obsolete root single-package manifests no longer present root `skills/` as
  current product truth.

# Verification Targets

- spec:core-and-playbooks-package-contract#ACC-004
- spec:core-and-playbooks-package-contract#ACC-006 — explicit no-runtime-claim / deferral only
- spec:core-and-playbooks-package-contract#ACC-007 — harness manifests/catalogs only

# Task For This Iteration

1. Create package-root Claude plugin metadata for core and playbooks.
2. Move or copy Claude core hook/context files into `loom-core` if the core package
   manifest refers to them; do not create playbook preload hooks that duplicate
   using-Loom doctrine.
3. Create package-root Codex plugin metadata for core and playbooks.
4. Create package-root Cursor plugin metadata for core and playbooks.
5. Create package-root Gemini extension metadata for core and playbooks, with core
   bootstrap references rooted inside `loom-core` and no unvalidated runtime claim.
6. Update root catalogs that already exist so they list `loom-core` and
   `loom-playbooks` as separate package roots.
7. Remove or neutralize obsolete root single-package manifests/bootstrap files if
   they would otherwise point at retired root `skills/` as current truth.
8. Run structural checks:
   - JSON syntax parse for every changed `.json` file;
   - verify package-root manifests use `./skills/` from inside their package root;
   - verify root catalogs mention both package roots;
   - grep changed harness surfaces for stale `./skills/`, `/skills/`, or
     `skills/using-loom` references that are no longer package-root-local;
   - run `git diff --check` for changed harness/package metadata paths.

# Verification Posture

Declared above as `observation-first`.

Before-state observation:

- Current root manifests and catalogs assume one repository-root package and/or
  root `skills/` paths.

After-state observation:

- Package-root manifests/catalogs exist and pass syntax/path checks.
- Root catalogs list the two package roots.
- No changed harness surface presents retired root `skills/` as current product
  truth.

# Stop Conditions

Stop and return `blocked` or `escalate` if:

- a needed edit would touch OpenCode files, public docs, examples, or source skills;
- a catalog format cannot truthfully list local package roots with the current
  evidence;
- Codex or Gemini runtime validation is needed to choose a metadata shape;
- the only way to make playbooks installable is to duplicate core skills or
  using-Loom preload files;
- JSON syntax or diff checks fail and cannot be fixed inside write scope.

# Output Contract

Return:

- outcome (`continue|stop|blocked|escalate`);
- files added/updated/deleted;
- package-root manifest summary by harness;
- root catalog source summary;
- before/after stale root `skills/` observations;
- JSON syntax/path check results;
- `git diff --check` result;
- quality delta achieved or not achieved;
- uncertainty discovered;
- self-review findings, risks, and maintainability concerns;
- ticket recommendation.

# Working Notes

Do not make public support claims for Codex hooks or Gemini two-extension behavior.
This packet only creates the structural package skeleton and explicit deferral
posture for runtime validation.

# Child Output

Outcome: `stop`.

Files added:

- `loom-core/.claude-plugin/plugin.json`
- `loom-core/claude-hooks/hooks.json`
- `loom-core/.codex-plugin/plugin.json`
- `loom-core/.cursor-plugin/plugin.json`
- `loom-core/gemini-extension.json`
- `loom-core/gemini-bootstrap.md`
- `loom-playbooks/.claude-plugin/plugin.json`
- `loom-playbooks/.codex-plugin/plugin.json`
- `loom-playbooks/.cursor-plugin/plugin.json`
- `loom-playbooks/gemini-extension.json`

Files updated:

- `.claude-plugin/marketplace.json`
- `.agents/plugins/marketplace.json`

Files deleted:

- `.claude-plugin/plugin.json`
- `.codex-plugin/plugin.json`
- `.cursor-plugin/plugin.json`
- `claude-hooks/hooks.json`
- `gemini-extension.json`
- `gemini-bootstrap.md`

Package-root manifest summary:

- Claude: core and playbook package-root manifests use local `./skills/`; core has
  package-local hooks and playbooks names the `loom-core` dependency without
  preload hooks.
- Codex: core and playbook package-root manifests use local `./skills/`; default
  prompts avoid claiming validated preload behavior.
- Cursor: core and playbook package-root manifests use local `./skills/` and
  playbooks names the `loom-core` dependency.
- Gemini: core extension points to package-local `gemini-bootstrap.md`; playbooks
  extension does not duplicate using-Loom bootstrap context and names the core
  dependency.

Root catalog summary:

- `.claude-plugin/marketplace.json` lists `loom-core` source `./loom-core` and
  `loom-playbooks` source `./loom-playbooks`.
- `.agents/plugins/marketplace.json` lists `loom-core` local path `./loom-core` and
  `loom-playbooks` local path `./loom-playbooks`.

Verification results:

- JSON parse passed for all changed JSON files.
- Package-root manifests use package-local `./skills/` where appropriate.
- Package roots and core using-Loom reference directories exist.
- Root catalogs mention both package roots.
- Obsolete root single-package manifests, root Claude hooks, and root Gemini
  bootstrap files are absent.
- Package-root trailing-whitespace scans passed.
- Scoped tracked-root `git diff --check` passed with no output.

Uncertainty discovered:

- No runtime validation was performed for Codex plugin preload behavior or Gemini
  two-extension install behavior.
- Cursor root marketplace was not added; package-root Cursor manifests were added
  and root catalogs updated only for existing root catalog surfaces.

Self-review risks:

- `.agents/plugins/marketplace.json` local package paths still need harness runtime
  validation.
- Metadata is intentionally skeletal and avoids public runtime support claims.

Ticket recommendation: parent should reconcile evidence and move the ticket to
mandatory critique / acceptance review.

# Parent Merge Notes

Consumed. Parent created `evidence:harness-package-surface-skeleton-check` from
the child output and independent structural checks. Mandatory critique initially
found a missing Cursor root marketplace; parent added `.cursor-plugin/marketplace.json`,
reran structural checks, and recorded final passing critique in
`critique:harness-package-surface-skeleton-review`.
