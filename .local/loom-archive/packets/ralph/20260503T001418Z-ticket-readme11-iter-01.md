---
id: packet:ralph-ticket-readme11-20260503T001418Z
kind: packet
packet_kind: ralph
status: consumed
target: ticket:readme11
mode: execution
change_class: documentation-explanation
risk_class: medium
style: reference-first
verification_posture: observation-first
iteration: 1
created_at: 2026-05-03T00:14:18Z
updated_at: 2026-05-03T00:19:24Z
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - ticket:readme11
    - evidence:readme-product-surface-framing-validation
    - packet:ralph-ticket-readme11-20260503T001418Z
  paths:
    - README.md
    - .loom/tickets/20260502-readme11-clarify-readme-product-surface.md
    - .loom/evidence/20260503-readme-product-surface-framing-validation.md
    - .loom/packets/ralph/20260503T001418Z-ticket-readme11-iter-01.md
parent_merge_scope:
  records:
    - ticket:readme11
    - evidence:readme-product-surface-framing-validation
    - packet:ralph-ticket-readme11-20260503T001418Z
  paths:
    - README.md
    - .loom/tickets/20260502-readme11-clarify-readme-product-surface.md
    - .loom/evidence/20260503-readme-product-surface-framing-validation.md
    - .loom/packets/ralph/20260503T001418Z-ticket-readme11-iter-01.md
source_fingerprint:
  git_commit: 2de8a2ec0f84a8867c1c3e223bc9d0216c774cd6
  integration_remote: origin
  integration_ref: origin/main
  integration_commit: 2de8a2ec0f84a8867c1c3e223bc9d0216c774cd6
  git_status_summary: clean
  compiled_from:
    - initiative:skills-corpus-template-grammar-safety-pass
    - plan:skills-corpus-template-grammar-safety-pass
    - ticket:readme11
execution_context:
  branch: main
  push_remote: origin
  worktree: /Users/alexanderbutler/code_projects/personal/agent-loom
  isolation: none
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: forbidden
context_budget:
  posture: tight
  max_source_files: 8
  max_excerpt_lines_per_file: 160
  avoid_full_file_reads: false
sources:
  initiative:
    - initiative:skills-corpus-template-grammar-safety-pass
  plan:
    - plan:skills-corpus-template-grammar-safety-pass
  ticket:
    - ticket:readme11
  records:
    - README.md
    - ARCHITECTURE.md
    - INSTALL.md
    - PROTOCOL.md
links:
  ticket:
    - ticket:readme11
---

# Mission

Clarify public README framing so `skills/` is named as the package product surface
and support docs, adapters, examples, packaging files, packets, memory, and saved
support artifacts cannot be read as protocol truth owners.

# Bound Context

This is the final ticket in `plan:skills-corpus-template-grammar-safety-pass` and
covers `initiative:skills-corpus-template-grammar-safety-pass#OBJ-011`. The plan
requires strict sequential execution, Ralph/fixer implementation, structural
evidence, mandatory oracle critique, retrospective disposition, semantic commit,
and push before the next plan-level council pass.

The project boundary is skills-only: Loom's distributable protocol surface is the
top-level `skills/` corpus. Support documents and adapter/example/packaging files
may explain, transport, or maintain the package, but they must not own protocol
truth.

# Source Snapshot

Baseline commit: `2de8a2ec0f84a8867c1c3e223bc9d0216c774cd6`, matching
`origin/main`. Worktree was clean before packet creation.

Relevant current README observations:

- `README.md:101` says Loom installs as a skills package and exposes `skills/` to
  a coding harness.
- `README.md:474-493` says the repository ships the Loom skill package but lists
  harness manifests/adapters, protocol docs, architecture notes, and examples in a
  way that can blur product surface versus support surfaces.
- `README.md:526-538` already labels `examples/` as internal fixtures and
  `skills/` as the canonical Loom skill package.
- `ARCHITECTURE.md:5-8` and `ARCHITECTURE.md:129-136` already state that Loom is
  not a runtime/CLI and that the product surface is `skills/`.

# Change Class

Declared as `documentation-explanation`; risk is medium because README public
framing can misrepresent protocol authority and product surface ownership.

# Verification Targets

- `initiative:skills-corpus-template-grammar-safety-pass#OBJ-011`
- `ticket:readme11#ACC-001`
- `ticket:readme11#ACC-002`
- `ticket:readme11#ACC-003`
- `ticket:readme11#ACC-004`

# Task For This Iteration

1. Capture before-state README searches for product-surface, `skills/`, support,
   adapters, examples, packaging, and truth-owner wording.
2. Update `README.md` only as needed so public framing clearly says `skills/` is
   the package product surface.
3. Keep support docs, adapters, examples, packaging files, packets, memory, and
   `.loom/support/` framed as explanatory, maintainer, transport, fixture, or
   operational support that does not own protocol truth.
4. Preserve public clarity; do not rewrite the README for style alone.
5. Do not change install mechanics, add command surfaces, add hidden helpers, or
   imply support artifacts are canonical owner layers.
6. Record `evidence:readme-product-surface-framing-validation` with before/after
   README framing searches and `git diff --check`.
7. Update `ticket:readme11` to `review_required`, link the evidence and this
   packet, update claim matrix statuses, and set next route to `critique`.
8. Fill this packet's `# Child Output`. The parent will mark the packet consumed
   after reconciliation and run mandatory oracle critique.

# Verification Posture

`observation-first`.

Capture before/after README searches for product-surface and support-surface
framing, then run `git diff --check`. The after-state must show `README.md`
clearly names `skills/` as product surface and does not imply support docs,
adapters, examples, or packaging files own protocol truth.

# Stop Conditions

Stop and return `blocked` or `escalate` if:

- the README fix requires changing install mechanics or adding a command surface;
- the fix would make `README.md`, `INSTALL.md`, `PROTOCOL.md`, `ARCHITECTURE.md`,
  examples, adapters, packaging files, packets, memory, or support artifacts own
  protocol truth;
- the fix would require product direction beyond the skills-only product boundary;
- the source fingerprint is materially stale before launch;
- unrelated files need editing to satisfy the ticket.

# Output Contract

Return outcome, files changed, records changed, before/after observation
commands/results, `git diff --check` result, residual risks, self-review
concerns, and ticket recommendation. Include whether the ticket should proceed to
mandatory oracle critique with profiles `product-framing`, `owner-boundary`, and
`operator-clarity`.

# Working Notes

Parent confirmed dependencies `ticket:routewf10`, `ticket:phsafe8`, and
`ticket:critrec9` are expected closed from prior pass summary and confirmed the
worktree was clean at packet compilation.

# Child Output

Outcome: `stop`.

Files changed:

- `README.md`
- `.loom/tickets/20260502-readme11-clarify-readme-product-surface.md`
- `.loom/evidence/20260503-readme-product-surface-framing-validation.md`
- `.loom/packets/ralph/20260503T001418Z-ticket-readme11-iter-01.md`

Records changed:

- `ticket:readme11`
- `evidence:readme-product-surface-framing-validation`
- `packet:ralph-ticket-readme11-20260503T001418Z`

Before observation commands/results:

- Confirmed source fingerprint with `git rev-parse HEAD` ->
  `2de8a2ec0f84a8867c1c3e223bc9d0216c774cd6`.
- Ran README `rg -n -i` searches for product-surface, `skills/`, support,
  adapters, examples, packaging, and truth-owner wording before editing.
- Before state showed `skills/` install and canonical package wording, existing
  support boundaries for packets/memory/`.loom/support/`, adapter wording as not
  a second doctrine source, and examples as non-product-surface fixtures; the
  `What ships` list still mixed `skills/` with support docs/adapters/examples and
  did not explicitly name packaging files as support or say these support surfaces
  do not own protocol truth.

After observation commands/results:

- Re-ran the same README searches after editing.
- After state shows `README.md:478` naming top-level `skills/` as package product
  surface, `README.md:487` naming `skills/` as package product surface and
  canonical skill corpus, `README.md:493` framing harness manifests/adapters as
  install/transport support rather than doctrine sources, `README.md:494` saying
  `PROTOCOL.md` is not a separate product surface, `README.md:496` naming
  packaging files/repository metadata as distribution and maintainer support, and
  `README.md:497` saying examples are not product-surface guidance or protocol
  truth.

Validation:

- `git diff --check` -> passed with no output.

Residual risks / self-review concerns:

- The change is public wording and should receive the mandatory oracle critique
  required by the ticket before acceptance.
- I did not change install mechanics, command surfaces, hidden helpers, runtime or
  schema enforcement, or owner layers.

Ticket recommendation:

- `ticket:readme11` is now `review_required`.
- Proceed to mandatory oracle critique with profiles `product-framing`,
  `owner-boundary`, and `operator-clarity`.

# Parent Merge Notes

2026-05-03T00:19:24Z: Parent accepted the bounded child output for mandatory
oracle critique and reconciled it into `ticket:readme11`,
`evidence:readme-product-surface-framing-validation`, and this packet. The child
stayed inside the declared write scope. Parent made the ticket coverage list
explicit for ticket-local `ACC-*` IDs and left the ticket in `review_required`;
closure remains blocked until mandatory oracle critique passes and the ticket
acceptance gate is updated.
