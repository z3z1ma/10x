---
id: "packet:ralph-ticket-xtt24452-20260507T223743Z"
kind: packet
packet_kind: ralph
status: consumed
target: "ticket:xtt24452"
mode: execution
change_class: config
risk_class: high
style: reference-first
verification_posture: observation-first
iteration: "01"
created_at: "2026-05-07T22:37:43Z"
updated_at: "2026-05-07T22:41:46Z"
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - "None - child returns implementation output only; parent reconciles Loom records"
  paths:
    - package.json
    - open-loom.mjs
    - loom-core/package.json
    - loom-core/open-loom-core.mjs
    - loom-playbooks/package.json
    - loom-playbooks/open-loom-playbooks.mjs
parent_merge_scope:
  records:
    - ticket:xtt24452
    - evidence:opencode-split-package-check
    - critique:opencode-split-package-review
  paths:
    - .loom/tickets/20260507-xtt24452-split-opencode-packages.md
source_fingerprint:
  git_commit: d021bc1c4968f398f6ca7a666e006b4cced6d666
  integration_remote: origin
  integration_ref: origin/main
  integration_commit: d021bc1c4968f398f6ca7a666e006b4cced6d666
  git_status_summary: dirty_mixed
  git_status_detail: "prior split tickets closed; package-root skill moves and harness skeleton changes are present; unrelated untracked examples remain"
  compiled_from:
    - ticket:xtt24452
    - spec:core-and-playbooks-package-contract
    - plan:split-core-and-playbooks-packages
    - decision:0008
execution_context:
  branch: main
  push_remote: none
  worktree: /Users/alexanderbutler/code_projects/personal/agent-loom
  isolation: none
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: forbidden
context_budget:
  posture: normal
  max_source_files: 12
  max_excerpt_lines_per_file: 140
  avoid_full_file_reads: true
sources:
  constitution:
    - decision:0008
  spec:
    - spec:core-and-playbooks-package-contract
  plan:
    - plan:split-core-and-playbooks-packages
  ticket:
    - ticket:xtt24452
    - ticket:7h8u6oxp
links:
  decision:
    - decision:0008
  ticket:
    - ticket:7h8u6oxp
---

# Mission

Split the OpenCode package surface into two package-root packages:
`open-loom-core` and `open-loom-playbooks`, while converting the repository root
package to private/non-published repo metadata.

# Bound Context

Governing source truth:

- `decision:0008` authorizes `loom-core/` and `loom-playbooks/` as the product
  package roots.
- `spec:core-and-playbooks-package-contract#REQ-010` requires OpenCode split work
  to target `open-loom-core` and `open-loom-playbooks`.
- `spec:core-and-playbooks-package-contract#REQ-015` requires the repository root
  package to be private/non-published after OpenCode split work.
- `ticket:xtt24452` excludes a compatibility meta-package, deprecation stub,
  public docs, and runtime validation in a real OpenCode install.

Current root OpenCode files to inspect:

- `package.json`
- `open-loom.mjs`

Current package roots to target:

- `loom-core/skills`
- `loom-playbooks/skills`

# Parent Launch Checklist

- Target ticket is active and Ralph-ready.
- Child write scope is limited to root/OpenCode package metadata and package-root
  OpenCode modules.
- Parent retains Loom record reconciliation.
- Network and destructive commands are forbidden.
- Verification posture is observation-first with package smoke and dry-run checks.

# Source Snapshot

The existing `open-loom.mjs` reads ordered using-Loom files and skill frontmatter
from package-local `skills/`. Reuse the smallest correct shape for package-root
modules rather than inventing a new runtime.

# Assumptions / Decision Triggers

| Assumption or question | Reversible? | Blocks child work? | Disposition |
| --- | --- | --- | --- |
| Root package becomes private repo metadata, not a third publishable package. | yes | no | accepted |
| Core package should preload using-Loom references and register core skills. | yes | no | accepted |
| Playbook package should register playbook skills only and should not preload core doctrine. | yes | no | accepted |
| Package dry-run shows missing package files or stale root `skills/` inclusion. | yes | yes | fix before returning |

# Change Class

Declared as `config`: this changes package metadata and OpenCode plugin modules.
Risk is high because it affects install behavior and the transition away from the
existing root `open-loom` package.

# Quality Delta

Baseline/current state:

- Root `package.json` describes a publishable `open-loom` package with root
  `skills/`, but root `skills/` has been retired.
- Root `open-loom.mjs` expects package-local root `skills/`.

Expected improvement:

- `loom-core` is a publishable OpenCode package named `open-loom-core` that
  registers core using-Loom references and core skills from `loom-core/skills`.
- `loom-playbooks` is a publishable OpenCode package named `open-loom-playbooks`
  that registers playbook skills from `loom-playbooks/skills` without core preload.
- Root `package.json` is private/non-published repo metadata with scripts for the
  split packages.
- Root `open-loom.mjs` no longer presents a current third OpenCode package.

# Verification Targets

- spec:core-and-playbooks-package-contract#ACC-005
- spec:core-and-playbooks-package-contract#ACC-007 — OpenCode package files/root package only

# Task For This Iteration

1. Add `loom-core/package.json` and `loom-core/open-loom-core.mjs`.
2. Add `loom-playbooks/package.json` and `loom-playbooks/open-loom-playbooks.mjs`.
3. Convert root `package.json` to private/non-published repo metadata and scripts
   that smoke-check both split packages.
4. Remove or neutralize root `open-loom.mjs` so it does not present root `skills/`
   as current OpenCode package truth.
5. Run structural checks:
   - `node loom-core/open-loom-core.mjs --smoke`;
   - `node loom-playbooks/open-loom-playbooks.mjs --smoke`;
   - inspect smoke output for core reference count and skill counts;
   - `npm --prefix loom-core pack --dry-run`;
   - `npm --prefix loom-playbooks pack --dry-run`;
   - inspect root `package.json` has `private: true` and no publishable main/export;
   - search changed OpenCode package files for stale root `skills/` product truth;
   - run `git diff --check` for changed OpenCode package files.

# Verification Posture

Declared above as `observation-first`.

Before-state observation:

- Root package describes publishable `open-loom` and root `skills/` files.

After-state observation:

- Core/playbook package smokes and dry-runs support the split package shape.
- Root package is private/non-published.
- No changed OpenCode package file presents root `skills/` as current product truth.

# Stop Conditions

Stop and return `blocked` or `escalate` if:

- a needed edit would touch public docs, examples, non-OpenCode harness manifests,
  or source skills;
- the package split requires a compatibility meta-package or deprecation stub;
- core smoke cannot register using-Loom references from `loom-core/skills`;
- playbook smoke requires copied core doctrine or core skill preload;
- package dry-run fails or includes wrong package files and cannot be fixed inside
  write scope;
- `git diff --check` fails and cannot be fixed inside write scope.

# Output Contract

Return:

- outcome (`continue|stop|blocked|escalate`);
- files added/updated/deleted;
- core package smoke result;
- playbook package smoke result;
- package dry-run summary for both packages;
- root private package inspection;
- stale root `skills/` / single `open-loom` package claim search results;
- `git diff --check` result;
- quality delta achieved or not achieved;
- uncertainty discovered;
- self-review findings, risks, and maintainability concerns;
- ticket recommendation.

# Working Notes

Use small package-local modules. Do not introduce a build step or hidden runtime.

# Child Output

Outcome: `blocked` on exact verification-command wording, with implementation
materially complete.

Files added:

- `loom-core/package.json`
- `loom-core/open-loom-core.mjs`
- `loom-playbooks/package.json`
- `loom-playbooks/open-loom-playbooks.mjs`

Files updated:

- `package.json`

Files deleted:

- `open-loom.mjs`

Core package smoke result:

- Passed.
- `pluginId: open-loom-core`
- `usingLoomReferenceCount: 8`
- ordered refs from `01-core-identity.md` through `08-trust-boundaries.md`
- `instructionCount: 8`
- `instructionsAreDeduped: true`
- `skillCount: 15`
- skill path: `loom-core/skills`

Playbooks package smoke result:

- Passed.
- `pluginId: open-loom-playbooks`
- `usingLoomReferenceCount: 0`
- `instructionCount: 0`
- `doesNotPreloadCoreDoctrine: true`
- `skillCount: 7`
- skill path: `loom-playbooks/skills`

Package dry-run summary:

- Exact requested commands `npm --prefix loom-core pack --dry-run` and
  `npm --prefix loom-playbooks pack --dry-run` returned success but packed the
  root private workspace package under npm `10.9.4`.
- Package-root dry-runs passed with `npm pack --dry-run ./loom-core` and
  `npm pack --dry-run ./loom-playbooks`.
- Package-local scripts passed with `npm --prefix loom-core run pack:check` and
  `npm --prefix loom-playbooks run pack:check`.
- Core dry-run package: `open-loom-core@0.1.10`, 101 files, includes
  `open-loom-core.mjs`, `package.json`, and `skills/`.
- Playbooks dry-run package: `open-loom-playbooks@0.1.10`, 27 files, includes
  `open-loom-playbooks.mjs`, `package.json`, and `skills/`.

Root private package inspection:

- `private: true`
- no `main`, `exports`, or `files`
- root scripts smoke and pack-check both split packages.

Search and diff results:

- No stale root `skills/` product-truth matches or single publishable `open-loom`
  package claim in changed OpenCode package files.
- Package-local `skills/` mentions appear only in `files` arrays for the two split
  packages.
- `git diff --check` passed with no output.

Quality delta achieved:

- Split OpenCode package modules exist.
- Root package is private/non-published.
- Root OpenCode module was removed.
- Core preloads doctrine; playbooks do not.

Uncertainty discovered:

- Parent must decide whether package-root dry-run evidence is sufficient instead
  of the exact `npm --prefix <pkg> pack --dry-run` command form.

Ticket recommendation: reconcile implementation as materially complete, document
the npm-prefix command nuance in evidence, and proceed to mandatory critique if
the parent accepts package-root dry-run evidence.

# Parent Merge Notes

Consumed. Parent accepted package-root dry-run evidence as the truthful proof for
package contents because the exact npm-prefix command packed the private root
workspace package in this environment. Parent recorded
`evidence:opencode-split-package-check`. Mandatory critique found and then verified
the playbook dependency metadata fix; final critique is recorded in
`critique:opencode-split-package-review`.
