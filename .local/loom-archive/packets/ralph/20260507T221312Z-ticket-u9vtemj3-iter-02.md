---
id: "packet:ralph-ticket-u9vtemj3-20260507T221312Z"
kind: packet
packet_kind: ralph
status: consumed
target: "ticket:u9vtemj3"
mode: execution
change_class: code-structure
risk_class: high
style: reference-first
verification_posture: observation-first
iteration: "02"
created_at: "2026-05-07T22:13:12Z"
updated_at: "2026-05-07T22:19:03Z"
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - "None - child returns implementation output only; parent reconciles Loom records"
  paths:
    - loom-playbooks/skills/**
parent_merge_scope:
  records:
    - ticket:u9vtemj3
    - evidence:skill-package-root-split-check
    - critique:skill-package-root-split-review
  paths:
    - .loom/tickets/20260507-u9vtemj3-split-skill-package-roots.md
source_fingerprint:
  git_commit: d021bc1c4968f398f6ca7a666e006b4cced6d666
  integration_remote: origin
  integration_ref: origin/main
  integration_commit: d021bc1c4968f398f6ca7a666e006b4cced6d666
  git_status_summary: dirty_mixed
  git_status_detail: "iter-01 moved skills into loom-core/ and loom-playbooks/; root skills removed; unrelated untracked examples remain"
  compiled_from:
    - ticket:u9vtemj3
    - spec:core-and-playbooks-package-contract
    - packet:ralph-ticket-u9vtemj3-20260507T220907Z
execution_context:
  branch: main
  push_remote: none
  worktree: /Users/alexanderbutler/code_projects/personal/agent-loom
  isolation: none
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: forbidden
context_budget:
  posture: normal
  max_source_files: 8
  max_excerpt_lines_per_file: 120
  avoid_full_file_reads: true
sources:
  constitution:
    - constitution:main
  spec:
    - spec:core-and-playbooks-package-contract
  ticket:
    - ticket:u9vtemj3
links:
  decision:
    - decision:0008
  packet:
    - packet:ralph-ticket-u9vtemj3-20260507T220907Z
---

# Mission

Clean up stale explicit playbook-to-core path references inside
`loom-playbooks/skills` after the package-root move.

Playbooks require `loom-core`; they must not contain file-path prose that implies
core skills live inside the playbook package's own `skills/` directory.

# Bound Context

`spec:core-and-playbooks-package-contract#REQ-005` says `loom-playbooks` requires
`loom-core` and must not duplicate core doctrine or core owner-layer skills.

`spec:core-and-playbooks-package-contract#ACC-003` expects no duplicated core
skills and clear dependency wording.

Ralph iteration 01 moved the directories and added top-level Core Dependency
sections, but parent verification found explicit references such as
`skills/loom-research/SKILL.md` inside playbook skills. These are stale as package
paths because `loom-playbooks/skills` intentionally does not contain core skills.

# Parent Launch Checklist

- Target ticket is active.
- Child write scope is limited to `loom-playbooks/skills/**`.
- Parent retains record reconciliation.
- Network and destructive commands are forbidden.
- Verification posture is observation-first.

# Source Snapshot

Use parent's before-state observation:

```text
rg -n 'skills/(using-loom|loom-workspace|loom-records|loom-memory|loom-ralph|loom-retrospective|loom-constitution|loom-initiatives|loom-research|loom-specs|loom-plans|loom-tickets|loom-evidence|loom-critique|loom-wiki)' loom-playbooks
```

This search found explicit core skill paths in `loom-spike`, `loom-codemap`,
`loom-ship`, `loom-debugging`, `loom-git`, and `loom-drive` skill prose.

# Assumptions / Decision Triggers

| Assumption or question | Reversible? | Blocks child work? | Disposition |
| --- | --- | --- | --- |
| Core skill names may remain as backticked skill names without `skills/...` path prefixes. | yes | no | accepted |
| Relative references to playbook-local files may remain when the file is inside `loom-playbooks/skills`. | yes | no | accepted |
| A playbook needs an actual core template path to be copied into its package. | yes | yes | stop; core duplication is forbidden |

# Change Class

Declared as `code-structure`: path/reference cleanup inside moved playbook skills
without changing workflow behavior.

# Quality Delta

Baseline/current state:

Playbook skills contain explicit `skills/loom-core-skill/...` file-path references
that are no longer package-local after the split.

Expected improvement:

Playbook skills refer to required core skills by skill name or package dependency
instead of stale package-local paths.

How the parent will judge the delta:

Parent will rerun the core-path search in `loom-playbooks` and inspect any
remaining results.

Known non-goals:

- No core skill edits.
- No public docs or manifests.
- No package metadata.

# Verification Targets

- spec:core-and-playbooks-package-contract#ACC-003
- spec:core-and-playbooks-package-contract#ACC-007 — moved playbook skill corpus only.

# Task For This Iteration

1. Replace explicit core skill file paths such as `skills/loom-research/SKILL.md`
   in `loom-playbooks/skills/**` with skill-name or core-package wording.
2. Preserve playbook-local file path references when they point to files within
   `loom-playbooks/skills`.
3. Run the before/after core-path search named above.
4. Run `git diff --check -- loom-playbooks`.

# Verification Posture

Declared above as `observation-first`.

Before-state observation: explicit core skill paths in playbook prose listed by
the parent search.

After-state observation: rerun search should produce no explicit core skill paths
inside `loom-playbooks` unless each remaining result is justified.

# Stop Conditions

Stop and return `blocked` or `escalate` if:

- an edit is needed outside `loom-playbooks/skills/**`;
- the only way to make a reference truthful is to duplicate core content;
- `git diff --check` fails and cannot be fixed inside write scope.

# Output Contract

Return outcome, files changed, before/after search observations, remaining risks,
`git diff --check` result, and ticket recommendation.

# Working Notes

Prefer concise wording like `the core loom-research skill` or `loom-research` over
long path explanations.

# Child Output

Outcome: `stop`.

Files changed:

- `loom-playbooks/skills/loom-codemap/SKILL.md`
- `loom-playbooks/skills/loom-debugging/SKILL.md`
- `loom-playbooks/skills/loom-drive/SKILL.md`
- `loom-playbooks/skills/loom-git/SKILL.md`
- `loom-playbooks/skills/loom-ship/SKILL.md`
- `loom-playbooks/skills/loom-spike/SKILL.md`

Before observation:

- Parent search found explicit core `skills/...` file paths inside playbook prose.

After observation:

- Rerunning the explicit core-path search inside `loom-playbooks` produced no
  output.
- Playbook-local references were preserved when they still pointed at files inside
  `loom-playbooks/skills`.

Remaining risks:

- Harness manifests, public docs, examples, package metadata, and runtime install
  validation remain outside this packet and ticket scope.

Ticket recommendation: reconcile evidence and mandatory critique, then close the
ticket if acceptance is satisfied.

# Parent Merge Notes

Consumed. Parent accepted the path cleanup, updated
`evidence:skill-package-root-split-check`, and recorded mandatory critique in
`critique:skill-package-root-split-review`. Ticket-owned finding dispositions and
acceptance closure are recorded in `ticket:u9vtemj3`.
