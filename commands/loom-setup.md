# loom-setup

Use this command to initialize or repair a Loom workspace. Trigger if the user says "set up loom", "initialize loom", "bootstrap loom", "create the loom directory", or similar workspace-setup requests.

This command is pure prompt guidance. It should be executable by the model from the visible Loom bundle and normal file tools alone. Do not depend on Python helper scripts to complete the setup.

## Goal

Create the full `.loom/` scaffold so the workspace has the expected directories and starter files for canonical records, packet runs, verification, memory, and harness invocation.

Prefer additive repair over replacement:

- create missing paths and files
- preserve existing Loom content unless the user explicitly asked to replace it
- if a file already exists, do not overwrite it just to match the templates below

## 0. Resolve Scope First

1. Resolve the workspace root using Loom's normal rule:
   - prefer the nearest ancestor containing both `.git/` and `.loom/`
   - if `.loom/` does not exist yet, use the current repository root
   - if the current working directory is a non-root subdirectory of a git repository and no established workspace exists above it, fail closed and ask the user which repository root should own the workspace
2. Inspect the current `.loom/` state before writing anything.
3. Treat existing records and memories as user data. Fill gaps; do not reset them.

## 1. Create The Full Directory Tree

Ensure these directories exist:

```text
.loom/
.loom/constitution/
.loom/constitution/decisions/
.loom/constitution/roadmap/
.loom/research/
.loom/initiatives/
.loom/specs/
.loom/plans/
.loom/tickets/
.loom/critique/
.loom/docs/
.loom/runs/
.loom/runs/ralph/
.loom/runs/critique/
.loom/runs/docs/
.loom/verification/
.loom/memories/
.loom/memories/system/
.loom/memories/user/
.loom/memories/glacier/
.loom/memories/glacier/system/
.loom/memories/glacier/user/
```

## 2. Seed Required Starter Files

Create each file below only if it is missing.

### `.loom/harness.md`

Use this starter content:

```markdown
# Harness Profiles

## default

General-purpose fresh child run profile. Replace the command below with the
harness invocation you actually want Loom to use in this workspace.

    opencode run -f "{{ packet_path }}" -- "{{ prompt }}"
```

### `.loom/constitution/constitution.md`

If the main constitution record is missing, create a starter scaffold with:

- JSON frontmatter between `---` fences
- `id: "constitution:main"`
- `kind: "constitution"`
- `links: {}`
- `repository_scope: {"kind": "repository", "repository_id": "repo:root"}`
- `schema_version: 1`
- `status: "active"`
- `created_at` and `updated_at` set to the current UTC timestamp

Use these body sections exactly:

```markdown
# Vision

TBD

# Principles

TBD

# Constraints

TBD

# Strategic Direction

TBD

# Current Focus

TBD

# Open Constitutional Questions

TBD

# Change History

- <today>: seeded initial Loom constitution scaffold
```

### `.loom/memories/manifest.json`

Use this exact structure:

```json
{
  "schema_version": 1,
  "domains": {
    "system": {
      "description": "Loom self-knowledge, workflow patterns, and memory-module health",
      "files": [
        "hot-memory.md",
        "patterns.md",
        "self-observations.md",
        "improvements.md"
      ],
      "label": "System self-memory and learned operating patterns",
      "path": "system",
      "triggers": [
        "memory pattern",
        "self-observation",
        "workflow improvement",
        "system behavior",
        "memory maintenance"
      ],
      "type": "system"
    },
    "user": {
      "description": "Non-canonical user context, reminders, entities, and observations",
      "files": [
        "hot-memory.md",
        "observations.md",
        "entities.md",
        "action-items.md"
      ],
      "label": "User context, named entities, and supporting reminders",
      "path": "user",
      "triggers": [
        "user context",
        "remember this",
        "entity",
        "observation",
        "reminder"
      ],
      "type": "user"
    }
  }
}
```

### `.loom/memories/hot-memory.md`

```markdown
<!-- L0: Cross-memory current state and top-of-mind pointers -->
# Loom Memory - Hot Memory

<!-- Rewrite freely. Keep this file small and point to domain files when detail grows. -->

## Watch

- Add cross-memory current-state pointers here as the workspace develops.
```

### `.loom/memories/link-index.md`

```markdown
<!-- L0: Backlink map showing which memory files point to which topics -->
# Memory Link Index

<!-- Auto-generated by housekeeping. Do not edit manually. -->
<!-- Last updated: YYYY-MM-DD -->

| Target | Linked from |
|--------|-------------|
| - | - |
```

### `.loom/memories/glacier/index.md`

```markdown
<!-- L0: Archive catalog for stored memory snapshots -->
# Memory Glacier Index

<!-- Auto-generated by housekeeping. Do not edit manually. -->
<!-- Last updated: YYYY-MM-DD -->

| File | Domain | Type | Tags | Date Range | Entries | Summary |
|------|--------|------|------|------------|---------|---------|
| - | - | - | - | - | 0 | No archived memory files yet. |
```

### `.loom/memories/system/hot-memory.md`

```markdown
<!-- L0: Current system-level priorities and memory pointers -->
# Loom Memory - System Hot Memory

<!-- Rewrite freely. Keep this file small and current. -->

## Watch

- Add system-level current-state pointers here as the workspace develops.
```

### `.loom/memories/system/patterns.md`

```markdown
<!-- L0: Distilled system and workflow patterns learned over time -->
# Loom Memory - System Patterns

<!-- Edit in place. Keep entries timeless and action-oriented. -->

## Active Patterns

- Add durable operating patterns here as they are learned.
```

### `.loom/memories/system/self-observations.md`

```markdown
<!-- L0: Append-only notes about system behavior that worked or failed -->
# Loom Memory - System Self Observations

<!-- Append-only. Format: - YYYY-MM-DD [tag]: observation -->
```

### `.loom/memories/system/improvements.md`

```markdown
<!-- L0: Candidate improvements for the memory module or workflow -->
# Loom Memory - System Improvements

<!-- Edit in place. Track concrete improvements and outcomes. -->

## Candidate Improvements

- Add memory or workflow improvement ideas here.
```

### `.loom/memories/user/hot-memory.md`

```markdown
<!-- L0: Current user-context priorities and reminders -->
# Loom Memory - User Hot Memory

<!-- Rewrite freely. Keep this file small and current. -->

## Watch

- Add current user-context pointers here as the workspace develops.
```

### `.loom/memories/user/observations.md`

```markdown
<!-- L0: Append-only timeline of non-canonical user observations -->
# Loom Memory - User Observations

<!-- Append-only. Format: - YYYY-MM-DD [tags]: observation -->
```

### `.loom/memories/user/entities.md`

```markdown
<!-- L0: Compact registry of people, orgs, and named user-context entities -->
# Loom Memory - User Entities

<!-- Edit in place. Format: ### Name / key facts / status:last:->[[link]] -->
```

### `.loom/memories/user/action-items.md`

```markdown
<!-- L0: Supporting reminders that do not belong in Loom tickets -->
# Loom Memory - User Action Items

<!-- Append new items and check off completed ones. -->
```

## 3. Repair Rules While Seeding

While creating the scaffold:

1. Do not overwrite an existing file just because it differs from the starter template.
2. If a required memory file exists but is empty, seed it with the matching starter content.
3. If a memory file exists without an L0 header, add the L0 header as line 1 and preserve the rest.
4. If `.loom/harness.md` exists, preserve the operator's profiles.
5. If `.loom/constitution/constitution.md` exists, preserve it unless the user explicitly asked for a reset.

## 4. Verify The Setup

After writing, verify that:

1. every directory in the tree above exists
2. every starter file above exists
3. every memory Markdown file starts with an `<!-- L0: ... -->` header
4. `.loom/memories/manifest.json` defines exactly the `system` and `user` domains with the expected file lists
5. `.loom/constitution/constitution.md`, if newly created, uses `constitution:main` and the expected section headings

If bundled validation scripts are available, it is good to run them after the file-based checks, but they are optional verification, not a dependency for this command.

## 5. Response Contract

When you finish:

1. list every directory you created
2. list every file you created
3. list any existing files you preserved unchanged
4. call out any malformed pre-existing files you could not safely repair without user guidance

Do not stop after creating only `.loom/` and the canonical subdirectories. This command is complete only when the starter file layer exists too, especially `.loom/harness.md` and the full memory scaffold.
