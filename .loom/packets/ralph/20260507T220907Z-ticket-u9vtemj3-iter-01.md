---
id: "packet:ralph-ticket-u9vtemj3-20260507T220907Z"
kind: packet
packet_kind: ralph
status: consumed
target: "ticket:u9vtemj3"
mode: execution
change_class: code-structure
risk_class: high
style: reference-first
verification_posture: observation-first
iteration: "01"
created_at: "2026-05-07T22:09:07Z"
updated_at: "2026-05-07T22:19:03Z"
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - "None - child returns implementation output only; parent reconciles Loom records"
  paths:
    - skills/**
    - loom-core/**
    - loom-playbooks/**
parent_merge_scope:
  records:
    - ticket:u9vtemj3
    - evidence:skill-package-root-split-check
    - critique:skill-package-root-split-review
  paths:
    - .loom/tickets/20260507-u9vtemj3-split-skill-package-roots.md
source_fingerprint:
  git_commit: d021bc1c4968f398f6ca7a666e006b4cced6d666
  integration_remote: origin
  integration_ref: origin/main
  integration_commit: d021bc1c4968f398f6ca7a666e006b4cced6d666
  git_status_summary: dirty_mixed
  git_status_detail: "tracked core prose edits from ticket:hi5e7nbr plus untracked unrelated example files and untracked Loom planning records"
  compiled_from:
    - ticket:u9vtemj3
    - spec:core-and-playbooks-package-contract
    - decision:0008
execution_context:
  branch: main
  push_remote: none
  worktree: /Users/alexanderbutler/code_projects/personal/agent-loom
  isolation: none
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: forbidden
context_budget:
  posture: normal
  max_source_files: 12
  max_excerpt_lines_per_file: 100
  avoid_full_file_reads: true
sources:
  constitution:
    - constitution:main
  initiative:
    - initiative:loom-install-experience
  research:
    - research:core-workflow-plugin-split-feasibility
  spec:
    - spec:core-and-playbooks-package-contract
  plan:
    - plan:split-core-and-playbooks-packages
  ticket:
    - ticket:u9vtemj3
links:
  decision:
    - decision:0008
  ticket:
    - ticket:hi5e7nbr
---

# Mission

Physically split the current top-level `skills/` corpus into the two package-root
skill trees required by `decision:0008` and
`spec:core-and-playbooks-package-contract`.

This iteration creates `loom-core/skills` and `loom-playbooks/skills`, moves the
exact skill directories into the correct package, and removes the root `skills/`
product surface by emptying it through moves. It does not update harness
manifests or public docs.

# Bound Context

Core membership from `spec:core-and-playbooks-package-contract#REQ-003`:

- `using-loom`
- `loom-workspace`
- `loom-records`
- `loom-memory`
- `loom-ralph`
- `loom-retrospective`
- `loom-constitution`
- `loom-initiatives`
- `loom-research`
- `loom-specs`
- `loom-plans`
- `loom-tickets`
- `loom-evidence`
- `loom-critique`
- `loom-wiki`

Playbook membership from `spec:core-and-playbooks-package-contract#REQ-004`:

- `loom-drive`
- `loom-git`
- `loom-debugging`
- `loom-spike`
- `loom-codemap`
- `loom-ship`
- `loom-skill-authoring`

The current `skills/` files contain tracked edits from closed `ticket:hi5e7nbr`.
Preserve those edits while moving directories.

# Parent Launch Checklist

- Target ticket is active and Ralph-ready.
- Child write scope is limited to `skills/**`, `loom-core/**`, and
  `loom-playbooks/**`.
- Parent retains Loom record reconciliation.
- Network and destructive commands are forbidden.
- Verification posture is observation-first with before/after structural scans.

# Source Snapshot

Read:

- `.loom/tickets/20260507-u9vtemj3-split-skill-package-roots.md`
- `.loom/specs/core-and-playbooks-package-contract.md`
- `.loom/constitution/decisions/decision-0008-core-and-playbooks-package-roots.md`

Before moving, list current `skills/*/SKILL.md` directories so membership can be
compared after the move.

# Assumptions / Decision Triggers

| Assumption or question | Reversible? | Blocks child work? | Disposition |
| --- | --- | --- | --- |
| Directory moves may use native shell `mkdir` and `mv`; prose/reference edits use `apply_patch`. | yes | no | accepted |
| Current root harness manifests may point at now-moved paths until a later ticket updates them. | yes | no | accepted; do not edit manifests |
| A moved skill reference can be made package-root-relative without changing semantics. | yes | no | accepted |
| A required path fix would touch public docs, examples, or harness manifests. | yes | yes | stop and report for later ticket |

# Change Class

Declared as `code-structure`: this is a source-layout migration and
behavior-preserving reference cleanup. Risk is high because it changes the product
surface and can break skill discovery if membership/path references drift.

# Quality Delta

Baseline/current state:

All shipped skills live under one top-level `skills/` tree.

Expected improvement:

The repository has two visible package roots with exact skill membership:
`loom-core/skills` and `loom-playbooks/skills`; root `skills/` no longer exists as
the product surface.

How the parent will judge the delta:

Parent will inspect package tree, skill frontmatter, membership, duplicate-core
absence under playbooks, root `skills/` absence, stale path search results, and
diff check output.

Known non-goals:

- No harness manifests.
- No public docs.
- No OpenCode package split.
- No runtime harness validation.

# Verification Targets

- spec:core-and-playbooks-package-contract#ACC-001
- spec:core-and-playbooks-package-contract#ACC-002 — structural/package inspection only.
- spec:core-and-playbooks-package-contract#ACC-003 — structural duplicate-core and dependency wording inspection only.
- spec:core-and-playbooks-package-contract#ACC-007 — moved skill corpus only.

# Task For This Iteration

1. Confirm current skill directory membership from `skills/*/SKILL.md`.
2. Create `loom-core/skills` and `loom-playbooks/skills`.
3. Move core skill directories into `loom-core/skills`.
4. Move playbook skill directories into `loom-playbooks/skills`.
5. Remove the root `skills/` directory if empty.
6. Search moved skill corpus for stale root-product-surface references to
   top-level `skills/`. Do not update public docs or manifests; only adjust moved
   skill prose when it clearly should be package-root-relative or package-role
   wording.
7. Run structural checks:
   - list `loom-core/skills/*/SKILL.md`;
   - list `loom-playbooks/skills/*/SKILL.md`;
   - verify root `skills/` is absent;
   - search for duplicate core skills under `loom-playbooks/skills`;
   - search for playbook skills under `loom-core/skills`;
   - run `git diff --check` on moved package roots.

# Verification Posture

Declared above as `observation-first`.

Before-state observation:

- Current root `skills/*/SKILL.md` membership.

After-state observation:

- Package-root skill membership lists.
- Root `skills/` absence.
- Duplicate/wrong-package membership searches.
- Stale root-path search summary in moved skills.
- `git diff --check` result.

# Stop Conditions

Stop and return `blocked` or `escalate` if:

- moving directories would overwrite existing `loom-core` or `loom-playbooks`
  contents;
- any skill directory is not listed by the spec membership;
- a necessary edit would touch files outside child write scope;
- a harness manifest or public doc must be edited to keep the repository usable;
- `git diff --check` fails and cannot be fixed inside child write scope;
- root `skills/` cannot be removed after the move.

# Output Contract

Return:

- outcome (`continue|stop|blocked|escalate`);
- files/directories moved;
- files changed by prose/reference cleanup;
- records changed (`None` expected);
- before-state membership observation;
- after-state package membership observation;
- stale path search summary;
- duplicate/wrong-package membership check results;
- `git diff --check` result;
- quality delta achieved or not achieved;
- uncertainty discovered;
- self-review findings, risks, and maintainability concerns;
- ticket recommendation.

# Working Notes

Keep this a source-layout migration only. Do not make package metadata guesses.

# Child Output

Outcome: `continue`.

Files/directories moved:

- Moved the 15 core skill directories from root `skills/` into
  `loom-core/skills/`.
- Moved the 7 playbook skill directories from root `skills/` into
  `loom-playbooks/skills/`.
- Removed the root `skills/` directory after emptying it through moves.

Files changed by prose/reference cleanup:

- Added `## Core Dependency` wording to playbook `SKILL.md` files.
- Initial stale-path scan still found explicit core `skills/...` paths inside
  playbook prose, so a follow-up iteration was recommended.

Records changed by child: None. Parent retained Loom record reconciliation.

Observed structural results:

- Before-state skill membership matched the known root `skills/*/SKILL.md` corpus.
- After-state package membership had 15 expected core skills and 7 expected
  playbook skills.
- Root `skills/` was absent.
- Duplicate/wrong-package membership checks found no overlap.

Uncertainty discovered:

- Explicit core skill file paths remained in playbook prose after the physical
  move. Parent compiled iteration 02 to clean those paths inside
  `loom-playbooks/skills/**`.

Ticket recommendation: continue with a bounded playbook path-cleanup iteration,
then reconcile evidence and mandatory critique.

# Parent Merge Notes

Consumed. Parent accepted the physical package-root move, created follow-up Ralph
iteration 02 for stale playbook-to-core path references, and preserved final
acceptance judgment for `ticket:u9vtemj3` until evidence and mandatory critique
were reconciled.

Critique later identified that this packet's child write scope over-granted
`loom-core/**` and `loom-playbooks/**` instead of only package `skills/**` paths.
Ticket disposition records this as an accepted scoped risk because source
inspection found no out-of-scope package metadata or manifest edits, and iteration
02 used a narrower write scope.
