---
id: packet:ralph-ticket-795fa0f4-20260502T104301Z
kind: packet
packet_kind: ralph
status: consumed
target: ticket:795fa0f4
mode: execution
change_class: protocol-authority
style: snapshot-first
verification_posture: observation-first
iteration: 1
created_at: 2026-05-02T10:43:01Z
updated_at: 2026-05-02T10:50:58Z
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records: []
  paths:
    - skills/loom-memory/**
    - skills/loom-records/**
    - skills/loom-retrospective/**
parent_merge_scope:
  records:
    - ticket:795fa0f4
  paths:
    - .loom/tickets/20260502-795fa0f4-clarify-memory-pruning-frontmatter.md
    - .loom/packets/ralph/20260502T104301Z-ticket-795fa0f4-iter-01.md
source_fingerprint:
  git_commit: 7e0b268868557542c9b7e937f0dbd2b8b93fa353
  integration_remote: origin
  integration_ref: origin/main
  integration_commit: 7e0b268868557542c9b7e937f0dbd2b8b93fa353
  git_status_summary: clean
  compiled_from:
    - initiative:skills-corpus-protocol-sharpening
    - research:skills-corpus-council-review
    - evidence:skills-corpus-council-review
    - plan:skills-corpus-protocol-sharpening
    - ticket:795fa0f4
execution_context:
  branch: main
  push_remote: origin
  worktree: /Users/alexanderbutler/code_projects/personal/agent-loom
  isolation: none
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: forbidden
context_budget:
  posture: normal
  max_source_files: 14
  max_excerpt_lines_per_file: 180
  avoid_full_file_reads: false
sources:
  constitution:
    - constitution:main
  initiative:
    - initiative:skills-corpus-protocol-sharpening
  research:
    - research:skills-corpus-council-review
  spec: []
  plan:
    - plan:skills-corpus-protocol-sharpening
  ticket:
    - ticket:795fa0f4
links: {}
---

# Mission

Clarify memory pruning, promotion, optionality, and frontmatter/status
expectations so memory remains useful support recall without becoming project
truth.

# Bound Context

Governing chain:

- `constitution:main` says canonical records own project truth and no hidden
  shadow ontology or runtime should become authoritative.
- `ticket:4e8ebe92` added the shared records memory exception: memory support
  files may use lightweight `<!-- L0: ... -->` headers instead of canonical YAML
  frontmatter, and optional memory metadata does not make memory canonical.
- `ticket:1a12d9ff` reinforced memory as optional support-only recall during
  resume and compaction.
- `ticket:233cfdeb` reinforced that scratchpad-like material belongs in owner
  layers, with memory only for support-only reminders, retrieval cues, or pointers.
- `ticket:795fa0f4` owns this bounded memory pruning and frontmatter precision
  slice.

Memory may help retrieval and continuity, but the canonical graph must remain
truthful when memory is absent, stale, or pruned.

# Source Snapshot

Current observations before launch:

- `skills/loom-memory/SKILL.md` already says memory is optional in the correctness
  sense, uses the index-card metaphor, requires one-line `<!-- L0: ... -->`
  headers for active memory files, and says promoted facts need canonical owners.
- `skills/loom-memory/references/memory-model.md` already defines the memory fit
  test, promotion/demotion routes, common files, and the size rule.
- `skills/loom-memory/references/housekeeping.md` already lists housekeeping
  triggers, including ticket closure and retrospective promotion, but could be
  more explicit about pruning versus promotion cadence and stale handling.
- `skills/loom-memory/references/retrieval.md` already says memory is a pointer
  surface and owner records decide truth.
- `skills/loom-records/references/frontmatter.md` already carries the memory
  support-layer exception for lightweight headers and optional local metadata.
- `skills/loom-records/references/status-lifecycle.md` already says memory support
  status is optional and retrieval-oriented when used.

# Change Class

Declared as `protocol-authority` and `risk_class: medium` because this changes
support-layer operator guidance and frontmatter/status expectations while
preserving the non-canonical memory boundary. Oracle critique is required by the
current parent workflow before acceptance.

# Verification Targets

- `ticket:795fa0f4` ACC-001
- `ticket:795fa0f4` ACC-002
- `ticket:795fa0f4` ACC-003
- `ticket:795fa0f4` ACC-004
- `ticket:795fa0f4` ACC-005
- `initiative:skills-corpus-protocol-sharpening#OBJ-003`
- `research:skills-corpus-council-review#CLAIM-007`

# Task For This Iteration

Implement the smallest coherent memory precision pass:

1. Clarify in `loom-memory` when memory should be pruned, promoted, linked, marked
   stale, or left alone.
2. Tie pruning/promotion cadence to existing owner workflows such as ticket
   closure, retrospective, rename/supersession cleanup, and memory housekeeping;
   do not create a separate cadence ledger.
3. Clarify frontmatter/status expectations: default memory files may stay
   lightweight with `<!-- L0: ... -->`; YAML frontmatter/status is optional local
   support metadata; validators must not require canonical `id`, `kind`, `scope`,
   or `links` just because memory exists.
4. Keep memory optionality framed as a correctness boundary: absent, stale, or
   pruned memory cannot make canonical truth false, but useful memory still reduces
   orientation cost.
5. Ensure memory does not become a live task ledger, evidence store, wiki,
   research log, or ticket substitute.
6. Add only targeted pointers in `loom-records` or `loom-retrospective` if needed
   for consistency; do not duplicate all memory guidance outside `loom-memory`.

# Verification Posture

`observation-first` structural validation.

Before editing, observe the current guidance shape with targeted searches for:

- `prune`, `promotion`, `promote`, `frontmatter`, `status`, `L0`, `optional`,
  `stale`, `second ledger`, `canonical truth`, `ticket closes`, and
  `retrospective` in the allowed skill surfaces.

After editing, run targeted checks for:

- pruning, promotion, linking, stale handling, and leave-alone guidance;
- frontmatter/status expectations aligned between `loom-memory` and
  `loom-records`;
- memory optionality as correctness boundary without calling memory useless;
- memory not acting as live task ledger, evidence store, wiki, research log, or
  ticket substitute;
- pruning/promotion cadence tied to ticket closure, retrospective, or housekeeping;
- no memory indexing, embeddings, database, script, or runtime requirement;
- `git diff --check`.

# Stop Conditions

Stop and return `blocked` or `escalate` if:

- the work appears to make memory canonical project truth;
- the work requires memory indexing, embeddings, databases, helper scripts, or a
  runtime;
- the work would require rewriting historical `.loom/memory` files;
- memory status would become a ticket-like execution state machine;
- editing outside child write scope seems necessary;
- destructive commands, Git config edits, commits, pushes, or remote mutations
  seem necessary.

# Output Contract

Return:

- outcome: `continue`, `stop`, `blocked`, or `escalate`;
- files changed;
- records changed;
- explicit memory structure/pruning rule;
- frontmatter/status expectation chosen;
- validation commands/searches and outcomes;
- self-review findings or concerns;
- blockers or residual risks;
- ticket recommendation.

# Working Notes

Parent will run oracle critique with operator-clarity and protocol-change lenses
before accepting or continuing.

# Child Output

Outcome: `stop`.

Files changed by child:

- `skills/loom-memory/SKILL.md`
- `skills/loom-memory/references/memory-model.md`
- `skills/loom-memory/references/housekeeping.md`
- `skills/loom-records/references/frontmatter.md`
- `skills/loom-records/references/status-lifecycle.md`
- `skills/loom-retrospective/SKILL.md`

Records changed by child: none.

Explicit memory structure/pruning rule:

- Memory stays as small linked cards.
- At ticket closure, retrospective promotion, rename/supersession cleanup, or
  housekeeping, choose whether to leave alone, link to an owner, mark stale,
  promote then simplify/remove, or prune.
- Prune only when obsolete, unverifiable, duplicated, low-signal, or no longer
  useful for retrieval.
- Promote first if pruning would remove the only durable truth.

Frontmatter/status expectation chosen:

- Default memory files may stay lightweight with `<!-- L0: ... -->`.
- YAML frontmatter/status is optional local support metadata only.
- Validators must not require canonical `id`, `kind`, `status`, timestamps,
  `scope`, or `links` for memory files.
- Memory status must not become canonical truth, live execution state, evidence
  sufficiency, or accepted explanation.

Validation reported by child:

- targeted searches confirmed leave/link/stale/promote/prune guidance;
- targeted searches confirmed frontmatter/status alignment in `loom-records`;
- targeted searches confirmed optionality as correctness boundary;
- targeted searches confirmed memory is not a live ledger, evidence store,
  research log, wiki substitute, or ticket substitute;
- targeted searches confirmed cadence tied to ticket closure, retrospective,
  rename/supersession cleanup, and housekeeping;
- `git diff --check` passed with no output.

Ticket recommendation: move to critique / acceptance review after parent
reconciliation.

# Parent Merge Notes

Accepted for review. Parent recorded validation in
`evidence:memory-pruning-frontmatter-validation` and moved `ticket:795fa0f4` to
review before oracle critique. Oracle critique passed with no findings, and
parent closed `ticket:795fa0f4` with residual risks recorded in the ticket and
critique.
