---
id: packet:ralph-ticket-ralphg20-20260503T080431Z
kind: packet
packet_kind: ralph
status: consumed
target: ticket:ralphg20
mode: execution
change_class: protocol-authority
risk_class: high
style: reference-first
verification_posture: observation-first
iteration: 1
created_at: 2026-05-03T08:04:31Z
updated_at: 2026-05-03T08:07:26Z
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - None - child returns output only; parent reconciles ticket, evidence, critique, and packet status.
  paths:
    - skills/loom-ralph/references/packet-contract.md
    - skills/loom-ralph/templates/ralph-packet.md
parent_merge_scope:
  records:
    - ticket:ralphg20
  paths:
    - .loom/tickets/20260503-ralphg20-add-ralph-launch-hard-gates.md
    - .loom/evidence/20260503-ralph-launch-hard-gates-validation.md
    - .loom/critique/ralph-launch-hard-gates-review.md
    - .loom/packets/ralph/20260503T080431Z-ticket-ralphg20-iter-01.md
source_fingerprint:
  git_commit: 7b7cb4bb4a77a139345585f741e611f7673f5e48
  integration_remote: origin
  integration_ref: origin/main
  integration_commit: 7b7cb4bb4a77a139345585f741e611f7673f5e48
  git_status_summary: dirty_mixed
  git_status_detail: parent-owned ticket and packet records are modified/untracked for launch; child write-scope files are clean relative to 7b7cb4b
  compiled_from:
    - ticket:ralphg20
    - ticket:gitstat26
    - plan:skills-corpus-context-integrity-hardening-pass
    - initiative:skills-corpus-context-integrity-hardening-pass
    - research:skills-corpus-third-pass-follow-up-validation
execution_context:
  branch: main
  push_remote: origin
  worktree: /Users/alexanderbutler/code_projects/personal/agent-loom
  isolation: none
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: forbidden
context_budget:
  posture: normal
  max_source_files: 8
  max_excerpt_lines_per_file: 180
  avoid_full_file_reads: true
sources:
  initiative:
    - initiative:skills-corpus-context-integrity-hardening-pass
  research:
    - research:skills-corpus-third-pass-follow-up-validation
  plan:
    - plan:skills-corpus-context-integrity-hardening-pass
  ticket:
    - ticket:ralphg20
    - ticket:gitstat26
  files:
    - skills/loom-ralph/references/packet-contract.md
    - skills/loom-ralph/templates/ralph-packet.md
    - skills/loom-records/references/route-vocabulary.md
    - skills/loom-tickets/references/readiness.md
links: {}
---

# Mission

Add Ralph launch hard gates for unresolved placeholders and target-ticket route
authorization.

# Bound Context

`ticket:ralphg20` covers `initiative:skills-corpus-context-integrity-hardening-pass#OBJ-021`.
Ralph packets are strong bounded contracts, but the current launch checklist does
not explicitly block launching a compiled packet that still contains unresolved
template placeholders, example IDs, or generic `<...>` tokens, and it does not
explicitly require the parent to verify that the target ticket authorizes the
Ralph route.

Route grammar lives in `skills/loom-records/references/route-vocabulary.md`.
Ticket readiness guidance says a Ralph-ready ticket names `ralph` as the next
route and makes the bounded iteration, write boundary, likely verification
posture, and expected output contract legible.

# Source Snapshot

- `skills/loom-ralph/references/packet-contract.md` has a parent launch checklist
  for source freshness, child write scope, parent merge scope, Git/execution
  context, verification posture, stop conditions, and output contract.
- `skills/loom-ralph/templates/ralph-packet.md` repeats the parent launch
  checklist in copyable packet form.
- Current checklist wording does not explicitly say unresolved placeholders,
  example IDs, or generic `<...>` tokens are launch-blocking for a saved compiled
  Ralph packet.
- Current checklist wording does not explicitly say the parent must verify that
  the target ticket's saved next-route truth authorizes `ralph` and that route
  readiness matches the packet.

# Change Class

Declared as `protocol-authority` with high risk. The edit changes Ralph launch
authorization guidance and must stay Markdown-native: no runtime, schema engine,
validator, command wrapper, generated index, or new owner layer.

# Verification Targets

- `initiative:skills-corpus-context-integrity-hardening-pass#OBJ-021`
- `ticket:ralphg20#ACC-001`
- `ticket:ralphg20#ACC-002`
- `ticket:ralphg20#ACC-003`
- `ticket:ralphg20#ACC-004`

# Task For This Iteration

Make the smallest coherent Ralph launch-gate edit:

1. Add launch-check guidance that a saved compiled Ralph packet must not be
   launched with unresolved template placeholders, generic `<...>` tokens, example
   IDs such as `ticket:<token>` / `spec:<slug>`, or placeholder-only fields that
   pretend to be real packet truth.
2. Add launch-check guidance that the target ticket's saved next-route truth must
   explicitly authorize `ralph`, and that Ralph-readiness fields match the packet's
   bounded iteration, write boundary, verification posture, and output contract.
3. Preserve parent-side packet authorization and ticket truth: if the ticket does
   not authorize Ralph or readiness does not match, the parent should reconcile the
   ticket or supersede the packet rather than letting the child infer authority.
4. Update only `skills/loom-ralph/references/packet-contract.md` and
   `skills/loom-ralph/templates/ralph-packet.md` unless you must escalate.
5. Do not change Ralph child outcome vocabulary.
6. Do not add an automated validator, schema engine, command wrapper, generated
   index, hidden helper, runtime enforcement layer, or new owner layer.

# Verification Posture

Observation-first. Before and after editing, report targeted observations for:

- unresolved placeholder / example ID / generic `<...>` launch-blocking wording;
- target ticket next-route authorization for `ralph`;
- readiness matching to bounded iteration, write boundary, verification posture,
  and output contract;
- parent-side ticket truth / packet authorization preserved;
- absence of validator/schema/runtime/command-wrapper/new-owner-layer
  requirements.

Run `git diff --check` after editing.

# Stop Conditions

Stop and return `blocked` or `escalate` if:

- satisfying the ticket would require an automated validator, schema engine,
  command wrapper, runtime enforcement layer, generated files, or a new owner
  layer;
- the change would let a packet overrule target-ticket truth;
- the change would alter Ralph child outcome vocabulary;
- the needed edits extend beyond the Ralph packet contract and Ralph packet
  template in a way that cannot be safely summarized for the parent.

Do not run `git fetch`, remote edits, Git config edits, destructive commands, or
shared Git metadata mutations. Do not edit parent-owned `.loom` records.

# Output Contract

Return outcome, files changed, records changed, before/after observations,
`git diff --check` result, self-review, blockers/residual risks, and recommended
ticket state/next route.

# Working Notes

Parent compiled from clean `main` at `7b7cb4b` after closing and pushing
`ticket:gitstat26`. Saving this packet and activating the ticket made the
worktree `dirty_mixed` before child launch; those parent-owned `.loom` records
are outside child write scope. The product files in `child_write_scope.paths`
remain clean relative to `7b7cb4b` at launch.

# Child Output

Outcome: `stop`

Files changed:

- `skills/loom-ralph/references/packet-contract.md`
- `skills/loom-ralph/templates/ralph-packet.md`

Records changed by child:

- None. Parent retained ticket, evidence, critique, and packet reconciliation.

Observed result:

- Before: parent launch checklists did not explicitly block saved compiled packets
  with unresolved placeholders, generic angle-bracket placeholder tokens, example
  IDs, or placeholder-only fields.
- After: both Ralph contract and template launch checklists include that
  launch-blocking guidance.
- Before: checklists did not require saved target-ticket next-route truth to
  authorize `ralph`.
- After: both checklists require the saved target ticket to explicitly name
  `ralph` as the authorized next route.
- Before: readiness matching was implied across separate scope/posture/output
  bullets, not tied to target-ticket truth.
- After: both checklists require ticket Ralph-readiness fields to match bounded
  iteration, child write boundary, verification posture, and output contract.
- Before: parent-side ticket truth existed elsewhere, but launch checklist did not
  explicitly prevent packet-over-ticket authorization.
- After: both files say the parent must reconcile the ticket or supersede the
  packet, and must not let the child infer authority from the packet or let the
  packet overrule ticket route truth.
- After: no automated validator, schema engine, runtime, command wrapper,
  generated index, hidden helper, or new owner layer was added or required.

Verification:

- `git diff --check` passed with no output.

Self-review:

- Edits stayed within `child_write_scope.paths`.
- No `.loom` records were modified.
- Ralph child outcome vocabulary was not changed.
- Change is Markdown guidance only.

Blockers or residual risks:

- No blockers.
- High-risk protocol-authority wording requires parent-side critique before
  acceptance.

Recommendation:

- Move `ticket:ralphg20` to `review_required` and route to mandatory critique.

# Parent Merge Notes

- 2026-05-03T08:07:26Z: Parent accepted the bounded implementation output, made a
  small wording adjustment so the saved packet template does not introduce
  self-blocking literal placeholder examples in the new launch-gate sentence,
  recorded `evidence:ralph-launch-hard-gates-validation`, marked this packet
  `consumed`, and moved `ticket:ralphg20` to `review_required` for mandatory
  critique.
