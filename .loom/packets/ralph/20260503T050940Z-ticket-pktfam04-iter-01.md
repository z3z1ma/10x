---
id: packet:ralph-ticket-pktfam04-20260503T050940Z
kind: packet
packet_kind: ralph
status: consumed
target: ticket:pktfam04
mode: execution
change_class: protocol-authority
risk_class: high
style: reference-first
verification_posture: observation-first
iteration: 1
created_at: 2026-05-03T05:09:40Z
updated_at: 2026-05-03T05:20:23Z
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - None - child returns output only; parent reconciles ticket, evidence, critique, and packet status.
  paths:
    - skills/loom-records/references/packet-frontmatter.md
    - skills/loom-ralph/references/packet-contract.md
    - skills/loom-ralph/templates/ralph-packet.md
    - skills/loom-critique/templates/critique-packet.md
    - skills/loom-wiki/templates/wiki-packet.md
parent_merge_scope:
  records:
    - ticket:pktfam04
  paths:
    - .loom/tickets/20260503-pktfam04-separate-packet-family-requirements.md
    - .loom/evidence/20260503-packet-family-requirements-validation.md
    - .loom/critique/packet-family-requirements-review.md
    - .loom/packets/ralph/20260503T050940Z-ticket-pktfam04-iter-01.md
source_fingerprint:
  git_commit: da8d30aedcd6c2dc3b89226e8d70068a9ef29aea
  integration_remote: origin
  integration_ref: origin/main
  integration_commit: da8d30aedcd6c2dc3b89226e8d70068a9ef29aea
  git_status_summary: clean
  git_status_detail: clean working tree at packet compile time
  compiled_from:
    - ticket:pktfam04
    - plan:skills-corpus-context-integrity-hardening-pass
    - initiative:skills-corpus-context-integrity-hardening-pass
    - research:skills-corpus-context-integrity-hardening-review
execution_context:
  branch: main
  push_remote: origin
  worktree: /Users/alexanderbutler/code_projects/personal/agent-loom
  isolation: none
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: forbidden
context_budget:
  posture: normal
  max_source_files: 8
  max_excerpt_lines_per_file: 140
  avoid_full_file_reads: true
sources:
  initiative:
    - initiative:skills-corpus-context-integrity-hardening-pass
  research:
    - research:skills-corpus-context-integrity-hardening-review
  plan:
    - plan:skills-corpus-context-integrity-hardening-pass
  ticket:
    - ticket:pktfam04
  files:
    - skills/loom-records/references/packet-frontmatter.md
    - skills/loom-ralph/references/packet-contract.md
    - skills/loom-ralph/templates/ralph-packet.md
    - skills/loom-critique/templates/critique-packet.md
    - skills/loom-wiki/templates/wiki-packet.md
links: {}
---

# Mission

Separate shared packet grammar from family-specific packet requirements for
`ticket:pktfam04` without weakening Ralph packet discipline.

# Bound Context

Shared packet grammar currently describes a full common shape and says fields such
as `source_fingerprint`, `execution_context`, and `context_budget` are required
for every current packet family. Critique and wiki templates include many of the
same fields, which can be useful but may pressure non-Ralph packets into fake
precision. Ralph must remain strict about source freshness, execution context,
child write scope, parent merge scope, and verification posture.

# Verification Targets

- `initiative:skills-corpus-context-integrity-hardening-pass#OBJ-005`
- `ticket:pktfam04#ACC-001`
- `ticket:pktfam04#ACC-002`
- `ticket:pktfam04#ACC-003`
- `ticket:pktfam04#ACC-004`

# Task For This Iteration

Make the smallest coherent packet-family edits:

1. Clarify shared packet fields versus family-specific required fields in
   `packet-frontmatter.md`.
2. Preserve strict Ralph requirements in `packet-contract.md` and
   `ralph-packet.md`.
3. Adjust critique and wiki packet template guidance if current wording implies
   Ralph-only requirements or fake precision for review/synthesis packets.
4. Do not add new packet families, new owner layers, or runtime validation.

# Verification Posture

Observation-first. Before and after editing, report targeted observations for:

- required shared fields versus family-specific fields;
- Ralph-only fields: `iteration`, `verification_posture`, strict freshness,
  execution context, and write-scope expectations;
- critique/wiki packet guidance that avoids fake precision while preserving target,
  source, write/merge, and reconciliation metadata;
- absence of new packet families, validators, schemas, or owner layers.

Run `git diff --check` after editing.

# Stop Conditions

Stop and report `blocked` or `escalate` if satisfying the ticket would require
weakening Ralph launch safety, dropping source/write/merge scope from Ralph,
adding a runtime validator/schema, or inventing a new packet family/owner layer.

# Output Contract

Return outcome, files changed, records changed, before/after observations,
`git diff --check` result, self-review, blockers/residual risks, and recommended
ticket state/next route.

# Working Notes

Parent compiled from clean `main` at `da8d30a` after closing `ticket:tplsave3`.

# Child Output

Outcome: stop - bounded packet-family requirement separation completed within
child write scope.

Files changed:

- `skills/loom-records/references/packet-frontmatter.md`
- `skills/loom-ralph/references/packet-contract.md`
- `skills/loom-ralph/templates/ralph-packet.md`
- `skills/loom-critique/templates/critique-packet.md`
- `skills/loom-wiki/templates/wiki-packet.md`

Records changed: none.

Before observations:

- Shared packet grammar treated `source_fingerprint`, `execution_context`, and
  `context_budget` as required for every current packet family.
- Ralph references already treated packet freshness, execution context, write
  scopes, and `verification_posture` as important launch-safety concerns, but
  some wording said "recommended" rather than "required".
- Critique and wiki packet templates carried the shared metadata shape, but fixed
  defaults such as `isolation: none` and command-policy values could imply
  precision that the review or synthesis parent had not actually established.
- The packet-family list remained limited to Ralph, critique, and wiki.

After observations:

- Shared packet grammar now distinguishes required identity/routing/scope/
  reconciliation fields from common support blocks whose requiredness and
  precision are family-owned.
- Shared grammar explicitly says Ralph requires `source_fingerprint`,
  `execution_context`, and `context_budget` as strict launch-safety metadata.
- Ralph packet contract now uses required language for source fingerprint,
  context budget, and execution context; the Ralph packet template calls
  `source_fingerprint`, `execution_context`, `child_write_scope`,
  `parent_merge_scope`, and `verification_posture` strict launch-safety fields.
- Critique and wiki packet templates now tell operators to use `unknown`, `none`,
  or explicit rationale when exact Git/execution details are unavailable or not
  material, and not to invent branch, remote, worktree, or command-policy
  precision.
- No new packet families, validators, schemas, runtime enforcement, or owner
  layers were added.

Validation:

- Targeted `rg` checks confirmed the shared/family split, strict Ralph safety
  wording, critique/wiki fake-precision guidance, unchanged current packet family
  list, and absence of forbidden runtime/owner-layer machinery.
- `git diff --check` passed with no output after adding this new packet with
  intent-to-add.

Self-review: edits are Markdown-only, stay inside the declared write scope, and
preserve Ralph launch safety while relaxing fake precision pressure for sibling
packet workflows.

Blockers/residual risks: no blockers. Parent still needs evidence, mandatory
critique, and ticket-owned acceptance review.

Recommendation: `review_required`; next route is parent evidence recording, then
mandatory critique.

# Parent Merge Notes

Accepted child output as in scope. Parent recorded structural evidence in
`evidence:packet-family-requirements-validation` and routed to mandatory critique.
Initial critique found a parent-record frontmatter vocabulary issue: this packet
used `change_class: packet-safety`, which is a critique profile. Parent repaired
the packet and ticket to valid `change_class: protocol-authority` before
critique re-review.
