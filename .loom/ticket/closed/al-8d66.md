---
"id": "al-8d66"
"status": "closed"
"deps": []
"links": []
"created": "2026-02-15T19:14:53Z"
"type": "task"
"priority": 1
"assignee": "z3z1ma"
"parent": "al-0463"
"tags":
- "sprint:Public-Launch-Architecture-Cleanup"
- "architecture"
- "launch"
- "cleanup"
"external": {}
---
# Sprint execution guardrail: overlap audit + architecture target map

## Objective restatement
Prevent duplicate refactors and merge churn by locking file-level ownership and execution lanes before launch cleanup implementation begins.

## Sprint focus
Own boundaries first, then execute architecture cleanup in parallel only where filesets are disjoint.

## Short sprint name proposal
Launch Boundary Lock

## Why this is the best next step
This sprint has three high-risk hotspots (`team/core.py`, `team/cli.py`, `workspace/cli.py`) plus active historical architecture work in adjacent areas. Without an explicit ownership map, workers will duplicate decompositions and create avoidable conflicts. Boundary-first execution maximizes throughput while reducing integration risk.

## Current state

### Backlog and direction
- Sprint ticket set (open): `al-f968`, `al-18ec`, `al-58c0`, `al-9bf8`.
- Current ticket (`al-8d66`) blocks `al-f968`, `al-18ec`, `al-58c0`.
- Team runtime currently has only integrator + this investigator active; no concurrent implementation workers are in-flight yet.

### Codebase architecture state relevant to this sprint
- `src/agent_loom/team/core.py`: **6404 LOC** (monolithic domain hotspot).
- `src/agent_loom/team/cli.py`: **1900 LOC** (single-file command + dispatch surface).
- `src/agent_loom/workspace/cli.py`: **2534 LOC** (single-file command + dispatch surface).
- Duplicate CLI output/serialization helpers exist across:
  - `src/agent_loom/team/cli.py` (payload/json result wrappers via `team.output`)
  - `src/agent_loom/workspace/cli.py`
  - `src/agent_loom/ticket/cli.py`
  - `src/agent_loom/compound/cli.py`
  - `src/agent_loom/pack/cli.py`
  - `src/agent_loom/init.py`

## Overlap matrix (required audit set)

| Ticket | Files touched (primary) | Intent | Conflict risk vs this sprint |
|---|---|---|---|
| `al-97e8` (review) | `src/agent_loom/core/{exec,fs,concurrent,time}.py`, `src/agent_loom/workspace/{utils.py,time_utils.py,duration.py,state.py}`, workspace/dashboard call sites | Move shared exec/fs/time/concurrency primitives into `agent_loom.core` | **Low** for current sprint hotspots; adjacent only via shared-core conventions |
| `al-33c8` (review) | `src/agent_loom/core/git.py`, `src/agent_loom/workspace/{git_ops.py,diff_ops.py,repo_ops.py}` | Consolidate git plumbing and dedupe workspace git helper layer | **Low** for team core/CLI decomposition; **Low-Med** adjacency to workspace CLI imports |
| `al-e024` (review) | `src/agent_loom/workspace/cli.py`, workspace package layout (`workspace/git/*`, `workspace/repo/*`, `workspace/harness/*`, `workspace/core.py`) | Reorganize workspace package + split CLI responsibilities | **High** overlap with `al-58c0` workspace CLI modularization lane |
| `al-1f81` (review epic) | Planning umbrella (workspace refactor/value prop) | Coordinates workspace child tickets | **Low direct**, but establishes prior decomposition intent that must not be contradicted |
| `al-0e62` (review) | `src/agent_loom/workspace/worktree_meta.py`, `src/agent_loom/workspace/lifecycle.py`, metadata touch call sites | Metadata storage/layout + lifecycle centralization | **Low-Med** adjacency to workspace CLI command handlers that call metadata operations |
| `al-0650` (in_progress) | `src/agent_loom/compound/*.py` including `compound/cli.py`, plus adapter/docs paths | Compound architecture simplification and CLI contract reshaping | **Med** overlap with `al-18ec` on `compound/cli.py` shared output helper migration |
| `al-c09c` (open) | `src/agent_loom/pack/cli.py`, `tests/test_pack_cli_ux.py` | Add `pack --diff` output path | **Med** overlap with `al-18ec` on `pack/cli.py` serialization/output helper area |

## Architecture target ownership map (authoritative)

### Lane A — Team core decomposition (ticket `al-f968`)
**Owns**
- `src/agent_loom/team/core.py`
- New decomposition modules under `src/agent_loom/team/` (or `src/agent_loom/team/core/` package), e.g. lifecycle/worker/inbox/merge/objective domains.

**Must not touch**
- Parser trees and command wiring in `src/agent_loom/team/cli.py` beyond import rewires needed to keep behavior stable.
- Any workspace CLI or shared CLI-output extraction surfaces.

### Lane B — Shared CLI output contract (ticket `al-18ec`)
**Owns**
- New shared helper module (target: `src/agent_loom/core/cli_output.py`).
- Serialization/emission helper migration in:
  - `src/agent_loom/team/cli.py`
  - `src/agent_loom/workspace/cli.py`
  - `src/agent_loom/ticket/cli.py`
  - `src/agent_loom/compound/cli.py`
  - `src/agent_loom/pack/cli.py`
  - `src/agent_loom/init.py`

**Must not touch**
- Argparse command tree restructuring.
- Business logic in `team/core.py`, workspace repo/harness modules, pack/compound/ticket domain logic.

### Lane C — Team/workspace CLI modularization (ticket `al-58c0`)
**Owns**
- `src/agent_loom/team/cli.py`
- `src/agent_loom/workspace/cli.py`
- New command-group modules under team/workspace CLI packages.

**Must not touch**
- `src/agent_loom/team/core.py` decomposition internals (except import/callsite rewires required by extracted handlers).
- Shared serialization primitive behavior beyond consuming what `al-18ec` provides.

### Lane D — Guardrails + readiness checks (ticket `al-9bf8`)
**Owns**
- Guardrail docs and checks/tests asserting boundaries from lanes A-C.

**Must not touch**
- Functional behavior of CLI commands or team/workspace core workflows.

## Ordering + parallelization (go/no-go guidance)

### Must run first
1. `al-8d66` (this ticket) — boundary lock.

### Safe parallel after `al-8d66`
2. `al-f968` (Lane A) and `al-18ec` (Lane B) can run in parallel **only with file fences above**.

### Must run after both A and B
3. `al-58c0` (Lane C) after `al-f968` and `al-18ec` complete.

### Finalization lane
4. `al-9bf8` (Lane D) after `al-f968`, `al-18ec`, and `al-58c0` complete.

### Conflict-avoidance rules
- If work requires editing a file owned by another lane, stop and handoff instead of expanding scope.
- `al-18ec` changes to `team/cli.py` and `workspace/cli.py` are limited to output helper extraction only.
- `al-58c0` must consume the shared output contract as-is; no local re-introduction of ad-hoc emit helpers.
- Any uncertainty on `workspace/cli.py` behavior parity defaults to existing `tests/test_workspace_cli_ux.py` as contract gate.

## Risks / unknowns and resolutions
- **Unknown:** review-state workspace tickets (`al-e024`, `al-0e62`) may already encode equivalent modularization decisions not obvious from current tree.
  - **Resolution:** treat existing repository state as source of truth; preserve existing workspace package boundaries and only refactor command-surface layering in `al-58c0`.
- **Risk:** shared output extraction changes JSON shape/format unexpectedly (indentation/envelope differences across CLIs).
  - **Detection:** CLI UX test diffs.
  - **Resolution:** centralize only primitive emit/payload logic; keep command-specific wrappers where envelope metadata differs.
- **Risk:** decomposition work in `team/core.py` and `team/cli.py` cross-contaminates during concurrent execution.
  - **Detection:** merge conflicts or import-cycle regressions.
  - **Resolution:** enforce lane fences, run targeted tests in each lane before integration.

## Verification
- `loom ticket show al-8d66`
- `loom ticket show al-0463`
- `loom ticket show al-f968`
- `loom ticket show al-18ec`
- `loom ticket show al-58c0`
- `loom ticket show al-9bf8`
- Confirm this ticket body includes: overlap matrix, ownership map, and ordering/parallelization guidance.

## Acceptance criteria
- Overlap matrix documented for all required tickets: `al-97e8`, `al-33c8`, `al-e024`, `al-1f81`, `al-0e62`, `al-0650`, `al-c09c`.
- File-level ownership map is explicit for launch cleanup hotspots.
- Parallelization and ordering rules are unambiguous for worker handoff.
- Parent ticket `al-0463` updated with concise boundary summary and execution lanes.

## Notes

**2026-02-15T20:23:19Z**

Major step complete: wrote full sprint brief + overlap matrix + authoritative ownership map into ticket body; added execution-boundary refinement notes to downstream sprint tickets (al-f968, al-18ec, al-58c0, al-9bf8); and posted concise boundary summary to parent al-0463. Next: final self-check on ticket set clarity, commit milestone, and send INVESTIGATOR_DONE to manager.

**2026-02-15T20:23:39Z**

Completion summary: finalized sprint guardrail package. Updated tickets: [al-8d66, al-0463, al-f968, al-18ec, al-58c0, al-9bf8]. Created tickets: none (existing sprint set was refined in-place). Recommended execution order: (1) al-8d66 gate complete, (2) run al-f968 + al-18ec in parallel with strict file fences, (3) run al-58c0 after both complete, (4) run al-9bf8 last. High-overlap watchlist for lane B: al-0650 (compound/cli.py) and al-c09c (pack/cli.py).

**2026-02-15T20:23:52Z**

Verification evidence: loom ticket show al-8d66 al-0463 al-18ec al-f968 al-58c0 al-9bf8; loom ticket list -T sprint:Public-Launch-Architecture-Cleanup; loom ticket list --status open; loom team status MiyagiDo; git status --short --branch; git log -n 20 --oneline; wc -l on sprint hotspot files; grep/read inspections on target CLI files for duplicate output helper definitions. Residual risk: medium overlap remains if al-0650/al-c09c modify compound/pack CLI helper regions concurrently with al-18ec; mitigation is explicit lane fence + rebase before merge.
