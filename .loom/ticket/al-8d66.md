---
"id": "al-8d66"
"status": "open"
"deps": []
"links": []
"created": "2026-02-15T19:14:53Z"
"type": "task"
"priority": 1
"assignee": "z3z1ma"
"parent": "al-0463"
"tags":
- "sprint:Public-Launch-Architecture-Cleanup"
- "architecture"
- "launch"
- "cleanup"
"external": {}
---
# Sprint execution guardrail: overlap audit + architecture target map

## Objective alignment
Before implementation, we need one authoritative map of what this sprint owns so workers do not duplicate in-flight/review refactors and create merge churn.

## Scope
- Audit overlap between this sprint and existing relevant tickets (`al-97e8`, `al-33c8`, `al-e024`, `al-1f81`, `al-0e62`, `al-0650`, `al-c09c`).
- Build a file-level architecture target map for launch cleanup work in:
  - `src/agent_loom/team/core.py`
  - `src/agent_loom/team/cli.py`
  - `src/agent_loom/workspace/cli.py`
  - duplicated CLI output helper locations (`team/cli.py`, `workspace/cli.py`, `ticket/cli.py`, `compound/cli.py`, `pack/cli.py`, `init.py`)
- Record ownership decisions and conflict-avoidance rules directly in ticket notes/body.

## Non-goals
- No production code edits.
- No behavior changes.
- No new CLI features.

## Implementation plan
1. Collect current ticket context:
   - `loom ticket show al-97e8`
   - `loom ticket show al-33c8`
   - `loom ticket show al-e024`
   - `loom ticket show al-1f81`
   - `loom ticket show al-0e62`
   - `loom ticket show al-0650`
   - `loom ticket show al-c09c`
2. Build overlap matrix with three columns per ticket: `files touched`, `intent`, `conflict risk`.
3. Define launch sprint ownership boundaries:
   - shared CLI output contract ownership,
   - team-core decomposition ownership,
   - team/workspace CLI decomposition ownership,
   - guardrails/docs ownership.
4. Publish explicit go/no-go guidance for downstream tickets:
   - “safe to run in parallel” lanes,
   - “must run after” dependencies,
   - “do not touch” files for each lane.
5. Add concise summary note back to parent `al-0463` with final boundary decisions.

## Verification
- `loom ticket show al-8d66`
- `loom ticket show al-0463`
- Confirm ticket body contains:
  - overlap matrix,
  - file-level ownership map,
  - ordering/parallelization guidance.

## Acceptance criteria
- Overlap matrix is documented for all listed relevant tickets.
- File-level ownership map is explicit and actionable.
- Parallelization/ordering guidance is unambiguous for worker handoff.
- Parent ticket `al-0463` is updated with the resulting ownership summary.

## Risks / edge cases
- **Risk:** Review-state tickets may already contain equivalent refactors in separate branches.
  - **Detection:** overlapping file targets and matching intent.
  - **Mitigation:** explicitly defer or narrow this sprint scope before implementation tickets begin.
- **Risk:** Incomplete overlap audit creates duplicate work.
  - **Detection:** worker asks for clarification on “who owns file X”.
  - **Mitigation:** require file-level ownership mapping before any code ticket enters in_progress.
