---
"id": "al-445a"
"status": "open"
"deps": []
"links": []
"created": "2026-02-16T05:13:19Z"
"type": "task"
"priority": 1
"assignee": "z3z1ma"
"tags":
- "sprint:YAML-Sprint-Foundations"
- "fanout"
"external": {}
---
# Sprint prep: YAML Sprint Foundations

## Sprint Brief

### Objective restatement
Deliver first-class sprint-aware ticket workflows and production-grade YAML team composition contracts so team orchestration is deterministic: clear member lifecycle (always-on vs ephemeral), explicit communication boundaries, and predictable spawn/worktree behavior.

### Sprint focus (2-5 words)
YAML Orchestration Contracts

### Why this sprint focus is the best next step
Recent commits already introduced core YAML roster loading, member resolution, broadcast routing, and sprint primitives in tickets. The highest leverage next step is hardening contracts at boundaries (schema/runtime/CLI/ticket precedence) so lower-quality workers can execute without ambiguity or hidden behavior. This reduces planning friction and prevents coordination regressions as roster complexity grows.

### Current state
- Existing tickets that matter:
  - al-445a (this sprint prep parent ticket)
  - No other open backlog tickets existed at sprint start.
- Codebase state that matters:
  - git status: clean branch except newly created sprint prep ticket artifact (`?? .loom/ticket/al-445a.md`).
  - Recent direction (`git log -n 20 --oneline`) shows active work on YAML composition/team routing and sprint plumbing:
    - feat(team): load and persist YAML composition at start
    - feat(team): enforce broadcast targets and comms policy routes
    - feat(team): apply YAML member resolution and BYO prompt wrapping
  - Key modules with active responsibility:
    - `src/agent_loom/team/composition.py` (YAML schema parse/validation)
    - `src/agent_loom/team/composition_runtime.py` + `src/agent_loom/team/core.py` (runtime role/member/workspace resolution + spawn behavior)
    - `src/agent_loom/ticket/core.py` + `src/agent_loom/ticket/cli.py` (sprint context, tag precedence, planning primitives)
    - Tests: `tests/test_team_composition_schema.py`, `tests/test_team_spawn_yaml_members.py`, `tests/test_team_sprint.py`, `tests/test_ticket_sprint_context.py`

### Plan overview
1) Lock communication/escalation schema contract in YAML parser and persistence model.
2) Implement strict runtime mapping for lifecycle/workspace/worktree semantics from roster.
3) Add operator-facing roster validate/inspect CLI commands to reduce misconfiguration loop time.
4) Harden ticket sprint context precedence and sprint planning primitives.
5) Add E2E sprint prep contract tests for architect fanout and dependency wiring expectations.

### Risks + unknowns (and resolution path)
- Schema drift risk between parser and runtime policy code.
  - Resolution: parser-driven normalized spec + focused contract tests at parse and runtime boundaries.
- Workspace mapping collisions/security risk (duplicate keys, path escape).
  - Resolution: deterministic key collision checks + existing path normalization constraints + regression tests.
- Sprint-tag precedence ambiguity (env vs stored context vs explicit flags) can hide tickets.
  - Resolution: codified precedence invariants and explicit test matrix in ticket module tests.
- E2E planning tests may overfit prompt wording instead of behavioral contract.
  - Resolution: assert required clauses/metadata structure, avoid brittle formatting assertions.

## Ticket Set

Created/updated ticket IDs:
- al-0c25 — Team YAML schema: communication policy + escalation contract
- al-2510 — Team runtime: member lifecycle/workspace mapping from YAML
- al-30d0 — Team CLI: roster inspect/validate command set
- al-b1cb — Ticket module: first-class sprint workflow hardening
- al-92ed — Sprint prep E2E: architect fanout + dependency plan contract

Dependencies encoded:
- al-2510 depends on al-0c25
- al-30d0 depends on al-0c25
- al-92ed depends on al-2510
- al-92ed depends on al-b1cb

Suggested ordering + parallelization:
- Wave 1 (sequential start):
  - al-0c25
- Wave 2 (parallel after al-0c25):
  - al-2510
  - al-30d0
  - al-b1cb (can start immediately; independent of team schema/runtime work)
- Wave 3 (fan-in after wave 2 criticals):
  - al-92ed

Proposed short sprint name:
- YAML Orchestration Contracts
