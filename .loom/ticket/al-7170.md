---
"id": "al-7170"
"status": "review"
"deps": []
"links": []
"created": "2026-02-11T23:48:39Z"
"type": "task"
"priority": 1
"assignee": "z3z1ma"
"tags":
- "memory"
- "scopes"
- "ux"
"external": {}
---
# Memory: command scope wildcard matching

Support glob-style command scope patterns (e.g. command:loom *, command:loom ticket *, command:ls *) so recall and filtering can match command families using fnmatch semantics.

## Notes

**2026-02-11T23:48:46Z**

Implementing command-scope wildcard support via fnmatch. Plan: keep command scope as kind=command, treat wildcard-bearing values as patterns during scope matching, preserve exact-match behavior, and add regression tests/docs examples for command:loom * and command:loom ticket *.

**2026-02-12T00:04:26Z**

Implemented command-scope wildcard matching via fnmatch semantics. command scopes now support glob patterns (e.g. command:loom *, command:loom ticket *, command:ls *) while preserving exact and substring behavior. Matching now handles both directions: note-pattern vs runtime command and runtime-pattern vs note command. Added regression tests for recall with command glob filters and note command patterns, and documented command wildcard support in README.

**2026-02-12T00:04:26Z**

Verification: uv run basedpyright (0 errors), uv run ruff check . (pass), targeted memory regressions (36 passed), full suite uv run pytest (230 passed).
