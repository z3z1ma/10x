{"instinct_candidates":[{"id":"compound-cli-change-requires-contract-tests","title":"Compound CLI changes must be pinned by contract tests","trigger":"When editing src/agent_loom/compound/cli.py (especially large refactors/simplifications) or behavior-adjacent modules like src/agent_loom/compound/sync.py","action":"Treat CLI output + generated artifacts as a contract: ensure deterministic ordering/formatting, update or add focused pytest coverage (prefer existing contract tests like tests/test_compound_install.py), and run the standard gates (uv run basedpyright, uv run ruff check ., uv run pytest <targeted>).","confidence":0.74,"tags":["compound","cli","testing","contracts","determinism"]},{"id":"large-deletion-refactor-requires-behavior-audit","title":"Large deletions imply explicit behavior audit","trigger":"When a diff removes hundreds of lines from a core module (e.g. src/agent_loom/compound/cli.py) or collapses multiple flows into fewer branches","action":"Write down the behavior invariants (inputs/flags -> outputs/side effects), ensure each invariant is covered by at least one test, and ensure error messages/exit codes remain intentional (no accidental silent success/failure).","confidence":0.63,"tags":["refactor","quality","cli-ux","testing"]},{"id":"docs-touch-means-ux-claims-check","title":"Docs touched => verify UX claims match reality","trigger":"When updating README.md or LOOM.md alongside functional changes","action":"Scan the edited docs for concrete promises (commands, flags, output snippets, workflow steps). Ensure those are still true given the code changes; if output is contract-tested, prefer docs wording that matches the contract-tested output.","confidence":0.58,"tags":["docs","ux","consistency"]}],"skill_candidates":[{"name":"compound-cli-contract-testing","description":"Keep Compound CLI behavior deterministic and protected by focused contract tests when refactoring src/agent_loom/compound/cli.py or src/agent_loom/compound/sync.py.","body":"# Compound CLI Contract Testing\n\nUse this when changing Compound CLI behavior (especially refactors) so UX + artifacts stay deterministic.\n\n## Scope\n- Applies to: `src/agent_loom/compound/cli.py`, `src/agent_loom/compound/sync.py` (and any Compound command wiring they influence)\n- Goal: deterministic output, stable semantics, and regression coverage\n\n## Checklist\n1. Identify the contract surface\n- CLI: command names, flags, help text, exit codes, stderr vs stdout\n- Output: headings/labels, ordering, whitespace, and any user-visible paths\n- Artifacts: any generated/installed files, rendered templates, or computed lists\n\n2. Make determinism explicit\n- Sort lists before printing\n- Use stable iteration order (avoid depending on dict insertion order unless guaranteed by construction)\n- Avoid timestamps/randomness in outputs unless explicitly part of the contract\n\n3. Update/add focused contract tests\n- Prefer extending existing tests that already pin Compound behavior (start by checking `tests/test_compound_install.py`)\n- Test both:\n  - happy path output/content\n  - failure modes (bad args, missing files, partial state) with intentional messages and exit codes\n\n4. Keep docs aligned when you touch them\n- If you edit `README.md` or `LOOM.md`, verify any example commands/outputs still match the contract behavior\n- Prefer language that mirrors contract-tested output when practical\n\n## Verify (local gates)\n- `uv run basedpyright`\n- `uv run ruff check .`\n- `uv run pytest tests/test_compound_install.py` (plus any new/adjacent tests you add)\n\n## Done when\n- Contract tests pin the behavior you intended to preserve\n- Gates are green\n- No accidental UX drift (output/exit codes) from the refactor\n","tags":["compound","cli","testing","contracts","determinism"],"source_instinct_ids":["compound-cli-change-requires-contract-tests","large-deletion-refactor-requires-behavior-audit","docs-touch-means-ux-claims-check"]}]}
